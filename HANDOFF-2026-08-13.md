# 交接记录 — 台湾半导体月度营收看板（2026-08-13）

Session id：`ceb85f99-be01-4d01-8f14-f72b1eccc569`
本机 transcript：`~/.claude/projects/-Users-hainan-Projects-monthly-op-dashboards/ceb85f99-be01-4d01-8f14-f72b1eccc569.jsonl`（2.7 MB）
子代理 transcript：同目录下 `ceb85f99-.../`（92 MB，含本轮 60+ 个 agent 的完整记录）

---

## 一、当前状态：全部已推、已上线

- 分支 `main`，本地与 `origin/main` **一致（0/0）**，工作区**干净**
- 最新 commit `4b0f201`「台湾半导体 7 家改用 TSM 图列：抽出 build/mrbase.py 底座，窗口拉到 2016」
- GitHub Pages 已构建完成，7 页线上 payload 与本地**逐字节一致**
- 站点：https://hzhan7.github.io/Monthly-OP-Dashboards/

半导体组 7 家（线上全部 `2026-07`）：

| slug | 公司 | 2026-07 月营收 | y/y |
|---|---|---|---|
| tsm | 台积电 2330 | NT$468bn | +45% |
| ase | 日月光投控 3711 | 73,784 NT$mn | +43.2% |
| mtk | 联发科 2454 | 48,475 NT$mn | +12.2% |
| nanya | 南亚科 2408 | 43,868 NT$mn | +719.6% |
| umc | 联电 2303 | 23,844 NT$mn | +19.0% |
| alchip | 世芯-KY 3661 | **US$230.7mn** | +156.4% |
| guc | 创意电子 3443 | 5,769 NT$mn | +158.4% |

---

## 二、架构（本轮建立）

```
build/mrbase.py      通用底座，产出 TSM 图列（1,749 行）
build/mrwin.py       窗口左端与排版的裁决层，可单测（python3 build/mrwin.py）
build/mrspecs/<t>.py 一家一份配置 + _facts.py（构建期现算工具）
build/<t>.py         7 个薄壳入口（python3 build/tsm.py 等）
fetch/<t>.py         各家抓取器，均有 latest_month() / update()
series/<t>.csv       真值序列
```

图列（Ex 编号在跳图的页上**压缩重排**，这是用户 2026-08-13 明确选定的）：

| | Ex1 | Ex2 | Ex3 | Ex4 | Ex5 | Ex6 | Ex7 | Ex8 | Ex9 | Ex10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 内容 | 汇总表 | 月营收+右轴12M滚动同比 | 聚合到季 | 环比 | 本币vs美元 | 汇率贡献 | 全历史 | NTD/USD | 同比热力 | 核对表 |
| tsm / alchip | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| ase/mtk/umc/guc | ✓ | ✓ | ✓ | ✓ | — | — | ✓ | — | ✓ | ✓ |
| nanya | ✓ | ✓ | ✓ | ✓ | — | — | ✓ | — | — | ✓ |

**路由**：`monthly_run.builder()` 认 `build/<t>.py` 优先、命中即 return。
`build/single.py` 已加单向守卫：`build/<t>.py` 里有 `mrbase` 字样的一律跳过
（否则人手跑 `single.py --all` 会把 6 页静默打回旧图列）。
`build/specs/{ase,mtk,nanya,umc,alchip,guc}.py` 已删；
`make_shells12.singles()` 的枚举源已改成 `specs/ ∪ mrspecs/`。

---

## 三、下一步：只剩两件事（已启动但被叫停，需重跑）

被叫停的 workflow run id：`wf_1dbd08f6-271`
脚本仍在：`~/.claude/projects/-Users-hainan-Projects-monthly-op-dashboards/ceb85f99-.../workflows/scripts/tw-ex8-and-nanya-heat-wf_1dbd08f6-271.js`
（可用 `Workflow({scriptPath, resumeFromRunId:'wf_1dbd08f6-271'})` 续跑；工作区当时是干净的，没有半成品）

### ① 5 家（ase / mtk / nanya / umc / guc）补 Ex8 汇率线

**依据**：Ex8 画的是**汇率本身**（`ds.fx`），不是任何营收量，不需要公司披露任何东西。
它现在是被 `build/mrbase.py:1076` 那一行和 Ex5/Ex6 捆在一起跳掉的：

```python
order = [s for s in _SLUGS if s not in skip
         and (has_fx or s not in ('fx_lines','fx_contrib','fx_rate'))]
```

**Ex5/Ex6 仍然不画**（那两张要美元营收线，这 5 家没有官方美元数）。
做法：spec 挂 `fx` + `skip: ['fx_lines','fx_contrib']`。

**必须一起修的三处耦合**（底座假定「有 fx 就一定有 fx_contrib」）：
- `build/mrbase.py:1630` 与 `:1637` 的 `R("fx_contrib")` → `KeyError: 'fx_contrib'`
- `build/mrbase.py:1794-1806` 核对表的外币列
- `build/mrbase.py:867` brief 的第 4 句

判据统一改成看 `EX` 里有没有那张图，而不是看 `ds.fx is not None`。

**读者现在损失了什么**（2026-07 实测）：

| | NT$ y/y | 汇率 y/y | US$ y/y | 汇率贡献 |
|---|---|---|---|---|
| mtk | +12.2% | +9.81% | +2.1% | **+10.0pp** |
| umc | +19.0% | +9.81% | +8.4% | +10.6pp |
| ase | +43.2% | +9.81% | +30.4% | +12.8pp |
| guc | +158.4% | +9.81% | +135.3% | +23.1pp |
| nanya | +719.6% | +9.81% | +646.4% | +73.2pp |

联发科头条 +12.2% 里 **10.0 个点是汇率**；汇率同比 12 个月内从 −10.1% 翻到 +9.8%。

**`usd_share_note` 用不带数字的定性版 + 官方 URL**（底座 `mrbase.py:380-385` 的校验文案
自己规定了这条退路）。各家表述互不相同，**不许互相继承、不许编百分比**：
- umc  `"More than half…primarily in U.S. dollars"`（无百分比）
- ase  20-F Item 5 `"The majority of our prices and revenues is denominated in U.S. dollars"`（原文与 SEC URL 已在 `build/mrspecs/ase.py` 里）
- mtk  `"nearly all of our revenue is in US dollar"`
- nanya 2025 年报 p.169 `"over 90%"`
- guc  **无任何比例表述可引** —— 只能写定性事实 + 出处

**数据边界**：`series/tsm_fx.csv` 覆盖 2016-01 起，够 mtk/ase/guc 直接用；
**umc/nanya 序列自 2013-01 起，需要把汇率回补到 2013**。
源就是仓库每月已经在下载的那一页：`fetch/tsm.py:108` 的 Fed H.10 `dat00_ta.htm`（覆盖 2000-01 起），
唯一的截断是 `fetch/tsm.py:128` 的 `SERIES_START = '2016-01'`。
口径：该月所有营业日 H.10 牌价的算术平均。**已入库的 2016-01 起那段一格都不许变。**

### ② 南亚科 Ex9 热力矩阵：不跳过，改口径

引擎限制属实：`assets/charts.js:521-536` 的 `heatScale()` 只读 `ex.matrix` 与 `ex.reverse`，
`t=(v-lo)/(hi-lo)` 线性双色，全文件 grep `vmin|vmax|clip|log(` 零命中；
`drawHeat()` 的 `fc = sc.at(+v)` 与 `txt(..., fmt(+v))` 吃同一个 v
⇒「色阶取对数、格内印原值」在 charts.js 上确实做不到。

**但「跳过」不成立**：本仓对同类情况的既定做法是「照出 + 加告诫」
（`build/mrspecs/mtk.py:381-388` 跑同一个判据，38% 的结论是「矩阵照出，但请按格内数字读」）。

实测（9 年窗口 103 格，判据 =「最宽 20% 色带里塞了几格」）：

| 矩阵内容 | 拥挤度 | 判定 |
|---|---|---|
| 单月 y/y（现口径） | **81%** | 读不出，属实 |
| **环比 m/m** | **38%** | **与 mtk 齐平，完全可用** |
| 对数增速 ln(1+g) | 45% | 可用，需一句口径注 |
| 3MMA 的同比 | 82% | ❌ 已实测证伪，没改善 |
| 只调 heat_years（2–12 逐个试） | 61%–85% | ❌ 无效 |

横比七家拥挤度：alchip 29% / ase 31% / guc 31% / tsm 33% / umc 34% / mtk 38% / **nanya 81%**
（nanya 是次差者的 2.1 倍，不是双标）。

**做法**：给底座加 spec 字段（如 `window.heat_metric ∈ {'yoy','mom','log_yoy'}`，默认 `'yoy'`），
nanya 用 `'mom'`，标题改成环比口径。这是 mrbase 的一个字段，不是 charts.js 的改动。

**顺带修**：nanya 现在 `skip_note` 引用的分位数（p5/p95、拥挤格数）算的是**全序列 151 个月**
（n=151、p5/p95=−59.9%/+404.9%、109 格 72%），而 `heatScale` 只从 matrix（9 年 103 格）算，
引擎实际会用的是 **−62.6%/+584.0%、83 格 81%**。引用的分位不是引擎会算出来的那个，
改成按实际 matrix 现算。

---

## 四、已知遗留（低优先，用户已说先不管）

1. **mtk / ase 按百万元整数落库**，其余四家保留千元精度。同组内不一致，两家图注都写明了，
   差全部 < 0.5 NT$mn（最大相对差 0.0034% / 0.0016%）。要统一得重抓这两家的源
   （CSV 里精度已丢失，改不了）。
2. **ase 的官方月度美元数还没落库**。ASEH 月度新闻稿逐月印 US$ 表
   （2026-07：合并 US$2,309mn、ATM US$1,487mn），落进 `series/ase.csv` 之后
   它可以走 `implied=False` 路径拿到**完整的 Ex5/6/8**，且比 TSM 的推导值更硬
   —— 这是七家里唯一有官方月度美元实绩的一家。
3. **`source_date` 只有 tsm / mtk / nanya 有**，ase / umc / alchip / guc 的 fetcher
   没往 `series/source_dates.csv` 写，页面抬头缺「官方发布于」那半句。
4. **热力矩阵 payload 里存 `0` 代替真值**（|y/y| < 0.5 的格）。各页图注都写明了、命中数也对，
   但替换发生在 payload 而不是渲染层：ase 2024-06 存 `0.0` 而真值是 **+0.434**。
   按数据读 `matrix` 的人会拿到不是 y/y 的数。
5. **mtk 的 `update()` 每次把 2018 起所有往年 PDF 设为必需**，将来某份 404 会硬失败
   挡住当月入库。核验判定是可接受取舍（换来全历史 103 格每月被主源复核，实测 25 秒）。
6. **文档/注释陈旧**：`monthly_run.py:11/29/63/66/83-86/395-398`、`build/roster.py:64/71/129`、
   `docs/CRON_WIRING.md §3-§5`、`README.md:58` 里的家数与路由描述都还是接入前的。
   6 个薄壳的 docstring 也还写着「`build/specs/<t>.py` 仍在仓库里」，而那 6 份已删。

---

## 五、本轮踩过的坑（别再踩）

- **MOPS 历史月营收表分两张**：`t21sc03_<roc>_<m>_0.html` 國內公司（980 家）、
  `_1.html` 外國公司 KY/F股（93 家）。世芯 3661、矽力 6415 只在 `_1`，用错后缀**静默漏掉整家**。
  域名是 `mopsov.twse.com.tw`，`mops.twse.com.tw` 已 404。每个请求约 60 秒。
- **护栏阈值要按表分档**：我自己写的回补脚本用 `len(rows) < 200` 判失败，
  那是照國內表（980 行）定的，把外國表（49–93 行）的 127 次**成功响应全判成失败**。
- **GUC 官方 xlsx 的 Total 行 115 格里 61 格是活公式**，`data_only=True` 读到的是
  Excel 写入的缓存值，上游换个工具另存就变 None 静默漏年 ⇒ 一律对分项求和。
- **GUC 新闻稿 slug 不可拼**（至少 7 种命名法，含官网自己拼错的 `202601revenuE`），
  不存在的 slug 是 **302 跳 /en**、跟随后 200 + 172,215 字节壳页，按状态码校验完全无效。
  壳页里**也有 'Hsinchu'**，有判别力的只有正文 `net sales`。
- **UMC 的 IR 年表有 Cloudflare**，实测单次成功率 7%–30%，主源必须是 SEC 6-K。
  6-K 的 EDGAR filingDate **只有 2025-05 之后**才与公告日同日，2013-2024 年最长滞后 33 天。
- **`gs_line` / `lines_endlabels` 吃不了 null**（走 `smooth()`，Catmull-Rom 会产出 NaN），
  必须显式截断；`gs_bar` / `qtr_bar` / `grouped_bars` 走 `polyline(doSmooth=false)`，
  留 null 会抬笔不画，安全。
- **`nanya` 这个 ticker 撞 payload 护栏正则**（`nan|inf` + 1-2 个字母），
  已在 `build/payload_guard.py` 加整词白名单 + `finditer`（避免白名单词在前把后面的坏串挡掉）。
- **`asx` 这个 slug 已被 ASX Limited 占用**，而日月光的 NYSE ADR 恰好也叫 ASX ⇒ 用 `ase`。
- **春节效应是伪命题**：实测 2 月 MoM，TSMC 十一年均值 −15.5%（11 年全负）比这几家都狠，
  TSM 图注里那句农历年归因是过度归因，通用模板应弱化而不是照抄。

---

## 六、每月更新

入口 `python3 monthly_run.py`（cron 每天跑一次）。7 家的 `fetch/<t>.py` 都齐了，
`TICKERS / LAG / META / GROUPS` 四项注册全部就绪（`monthly_run.check_registry()` 实测「名单一致，无告警」）。

⚠️ `guard_dirty_tree()` 只允许 `data/` 与 `series/` 有未提交改动，
其余目录一脏就整轮 `FAILED` + `exit(1)`，28 家一起不发 —— 改完代码记得提交。
