# 总仓月度看板 —— 依赖锁定
#
# 口径：这里写的是**本机实测跑通**的版本（Python 3.12.12，/Users/hainan/.local/bin/python3，
# 2026-08-05 全量跑过 monthly_run.py）。不是「最低可用版本」，也不是「最新版本」。
# monthly_run.py 启动时会拿本文件跟实际装的版本对一遍，不符只告警不拦（见 check_deps）。
#
# 为什么这个文件存在：2026-08-04 本机 pandas 被静默升到 3.0.5，COST 这条链当天 100% 挂掉
# （详见下面 pandas 那段）。挂之前没有任何一处声明过「这套代码在哪个版本上验过」，
# 于是故障表现成「Costco 官网是不是改版了」，排查方向整个错。锁定 + 启动自检就是为了
# 下次同样的事发生时，第一屏就告诉你「版本变了」，而不是让你去翻新闻稿的 HTML。
#
# 装法：
#     python3 -m pip install -r requirements.txt
# 改版本前先跑一遍：
#     python3 monthly_run.py --force --only cost,ibkr,lpla,cme      # 覆盖 pandas/fitz/pdfplumber/openpyxl 四条解析路径
#
# ── 谁在用 ────────────────────────────────────────────────────────────────────

# pandas —— 28 个模块（fetch/* 的解析、build/* 的序列对齐与 Period 索引）；
#   COST 的解析管道（fetch/cost_release.py 的 pd.read_html）曾住在 /COST月度销售 skill 里、
#   由 skill 与本仓库共用；skill 已删除、代码已搬进本仓库，所以「两边要同版本」这条约束没了。
#
#   ⚠️ 3.0 是**行为破坏性大版本**，别顺手升也别顺手降：
#   1. `astype(str)` 产出 StringDtype(storage='python', na_value=nan)，**NaN 仍是 float**，
#      不再像 2.x 那样被转成字符串 'nan'。凡是 `astype(str)` 之后直接 ' '.join / 假定元素都是 str
#      的地方，在 3.0 下必抛 TypeError。COST 的 comp 表解析
#      （fetch/cost_release.py 的 parse_release）就是这么死的 —— GlobeNewswire 的表必然带空单元格，
#      所以是 100% 复现，不是偶发。现已在那里补 `.fillna('nan')` 修好。
#   2. read_csv / read_html 读出的文本列默认是 StringDtype 而不是 object dtype。
#      任何 `dtype == object` 的判断在 3.0 下都不再成立（本仓库目前没有这种写法，别新写）。
#   3. Copy-on-Write 成为默认且不可关闭，链式赋值（df[a][b] = v）静默无效不再报警。
#      本仓库目前没有链式赋值（已核查），新增代码请一律用 .loc 单步赋值。
#   降到 2.x 同样要重跑一遍：上面那处 `.fillna('nan')` 在 2.x 下是无害 no-op（2.x 的
#   astype(str) 已经把 NaN 变成 'nan' 了），所以 2.x/3.x 双向兼容 —— 但没在 2.x 上实测过。
pandas==3.0.5

# numpy —— 26 个模块。build/* 全部的数值窗口/百分位/趋势线计算走它。
#   pandas 3.0.5 要求 numpy>=1.26；2.x 系列本身没在这套代码里踩过坑，
#   跟 pandas 一起动就行，别单独升（numpy 2.x 的标量 repr 变化会影响 CSV 回写格式）。
numpy==2.5.1

# openpyxl —— 解析官方 xlsx：fetch/{cme,cboe,hood,schw,spgi,tsm}.py + build/extract_hood.py。
#   六家都是延迟 import 并带明确报错（见 fetch/cme.py:334-337），所以没装时是「那几家 FAIL」
#   而不是整个脚本崩 —— 但那等于半数看板停更，实际必装。
openpyxl==3.1.5

# xlrd —— **2026-08-19 新增**。只有一处用：fetch/lseg_primary.py 的 `_xlrd()`，
#   读 LSE AIM factsheet 2016-10 及更早的 .xls（BIFF 二进制，openpyxl 读不了）。
#
#   为什么加这条依赖，而不是用本机已有的 `soffice --convert-to xlsx`：
#     · xlrd 是纯 Python、无扩展模块、装完约 100KB，且**已经在本机**（2.0.2）；
#       soffice 不用加依赖，但每转一个文件要起一次 LibreOffice 进程（本机实测冷启
#       10s 量级），无人值守下还多两类新故障——用户级 profile 锁（同一台机上
#       monthly_run 与人手操作撞车就挂）与转档失败时留下的僵尸进程。
#       cron 里为了 12 个历史文件引入一个重进程，性价比是负的。
#     · 转档还多一层「LibreOffice 怎么理解这张表」的不确定性；xlrd 是直接读 BIFF
#       记录，拿到的就是文件里那个浮点数。本模块所有闭合检验都是逐位比对，
#       多一层重排版就多一处解释不清的差异。
#   xlrd 2.0 起**只支持 .xls**（不再读 xlsx），这正好是这里要的：xlsx 那一支归
#   openpyxl，两者职责不重叠，不会出现「同一个文件两个库读出两个结果」。
#
#   ⚠ 它**不是可选依赖**。缺了它，fetch/lseg_primary.py 会在 2016-01 那一期直接
#   raise（`_xlrd()` 里有明确报错），而不是悄悄少写 12 行 —— 因为合流层
#   fetch/lseg.py 的 `_refresh_primary` 是整表重写 part CSV 的，静默少行会把已经
#   入库的 12 行从 series/lseg_part_primary.csv 里抹掉。
#   改版本前重跑：python3 -c "import importlib.util as u; \
#     m=u.module_from_spec(s:=u.spec_from_file_location('p','fetch/lseg_primary.py')); \
#     s.loader.exec_module(m); m.write_csv('series', m.fetch_rows('cache'))"
#   然后确认 series/lseg_part_primary.csv 字节级不变。
xlrd==2.0.2

# lxml —— **没有任何一行代码直接 import 它，但它是硬依赖**，最容易在重装环境时漏掉。
#   fetch/cost_release.py 的 `pd.read_html()`（parse_release 里解 comp 表）需要一个 HTML parser 后端。
#   pandas 的 flavor=None 顺序是 lxml → bs4+html5lib；本机 html5lib **没装**，
#   实测 `read_html(flavor='bs4')` 直接 ImportError。也就是说 lxml 掉了就没有后备，
#   COST 整条链立刻死，且报错信息是「Import html5lib failed」——跟 Costco 毫无关系，极难联想。
lxml==6.1.1

# pymupdf (import 名 fitz) —— IBKR 的历史指标 PDF：fetch/ibkr.py:68、build/ibkr.py:162，
#   HKEX 的费率 PDF：fetch/rates_hkex.py:129（这个是模块顶层 import，没装直接 ImportError），
#   以及 fetch/ibkr_source.py（下载与解析管道，build/ibkr.py 也复用它）——
#   那份代码原先在 ~/.claude/skills/IBKR月度指标/build_report.py 里，skill 已删除、代码已搬进本仓。
pymupdf==1.28.0

# pdfplumber —— 只有 fetch/lpla.py:204 用（LPL 的月度 PDF 新闻稿），延迟 import 带明确报错。
#   会连带装 pdfminer.six / cryptography / pillow，是本清单里最重的一条。
pdfplumber==0.11.10

# matplotlib —— 本仓库的**网页看板路径不用它**（data/*.js 是纯 JSON payload，图在浏览器里画）。
#   仍然必须装，因为还有一处在用：
#     · build/gsx.py（GS exhibit 版式绘图库）+ build/build_*.py 这批出 PDF 的旧脚本，monthly_run.py 不调，但仍在仓库里且能跑
#   （原来还有第二条「/COST月度销售 与 /IBKR月度指标 两个 skill 的 build_report.py 靠它」，
#    两个 skill 已删除，那条随之作废 —— 但 matplotlib 不能因此摘掉，上面那条仍然成立。）
#   中文字体只有 Hiragino Sans GB 可用（本机无 YaHei/PingFang 可靠回落）。
matplotlib==3.11.1

# curl_cffi —— **可选**。fetch/hood.py:134 的首选下载通道（impersonate='chrome' 过 Robinhood 的
#   TLS 指纹墙）；装不上会自动回落到通道 2 的 nscurl（fetch/hood.py:161 的 for 循环），
#   所以缺它不会让 hood 挂。启动自检对这一条只提示、不当成异常。
curl_cffi==0.16.0
