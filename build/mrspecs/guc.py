# -*- coding: utf-8 -*-
"""创意电子 GUC（3443.TW）月度营收页配置 —— 跑在通用底座 `build/mrbase.py` 上。

本文件只有**数据与本家专属的事实**，没有图型逻辑（图型在 `build/mrbase.py`，
窗口左端与排版裁决在 `build/mrwin.py`），也没有一行「if ticker ==」式的分支。

━━ 与 /tsm/ 图列的对照（本家出九块：缺的两张是**结构性**缺，多的那张也是）━━━━
⚠️ 这张表**按 slug 对，不按编号**。上一版按编号写（「TSM 的 Ex7 → 本页 Ex5」那种），
   底座插进一张 `mix` 之后整列右移一格，全表当场变成错的指路牌 —— 正文早就立了
   「本文件一个 Exhibit 编号都不写」的规矩，这张表是当时漏掉的那一处，本轮补上。
    slug        /tsm/  本页   本页的处置
    （汇总表）   出     出    同，且**多两行**：官方逐月给的 Turnkey / NRE & Others 拆分
    rev_bar      出     出    同（右轴 12 个月滚动合计同比），且**本轮起按分部堆叠**：
                             柱高仍是合并营收、柱顶那个数仍是合计。堆叠把两类收入
                             并成的那一段画成一整块，这个视觉暗示由 note_extra 拆掉
    qtr          出     出    同
    mom          出     出    同
    mix          —      出    **本页独有**：/tsm/ 月度只公布一个合并数，没有分部列，
                             这张图在它身上不存在对象。分部列的来历底座不兜底，
                             必须由本文件的 note_extra['mix'] 讲清（底座只说占比是现算的）
    fx_lines     出     —     **显式 skip**：要一条美元营收腿，本家没有官方美元数
    fx_contrib   出     —     **显式 skip**：在本家它退化成汇率自己的重画（见 D）
    hist         出     出    同，且**多两条线**：Turnkey / NRE & Others 与合并线同图
    fx_rate      出     出    **照出**：画的是汇率本身，不需要公司披露任何东西（见 D）
    heat         出     出    同
    （核对表）   出     出    同，且**多两列**（两条分部列的官方原始单位）与一列
                             NTD/USD（汇率那张图的逐月读数）；另有一列「本月備註
                             （MOPS 原文）」由底座按近 N 个月有没有备注**自动**加，
                             不由本文件控制，本文件也不许假设它一定在
编号不是写死的：底座按「哪几张图真的画出来了」现算 `EX` 表，页内所有「见 Exhibit X」
都查它。**本文件因此一个 Exhibit 编号都不写**（写了就会在别人加图那天变成错的指路牌
—— 上面那张对照表就是这么坏掉过一次的，别再往回写编号）。

━━ 官方源核实记录 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
下面每一条都写清楚「从哪个文件的哪一段读到的」，因为本页的多个字段
（`value.summable` / 没有 `fx` / 没有 `official_yoy` / `segments` 的口径）全押在这几条上。

A. **单位与精度：官方是新台币千元（NT$K）。**
   IR「Historical Monthly revenue」xlsx 的表头逐字是 `Unit: NT$K `。
   `series/guc.csv` 的三列都叫 `*_ntd_mn`，但每一格都带 **3 位小数** ——
   那 3 位不是估计值，是官方 NT$K 除以 1000 的原位搬运，**逐位无损**。
   ⇒ `value.raw_dec = 3`：核对表印到小数点后三位，读者看到的每一位都是官方数字；
     少印一位就是把官方的千元位四舍五入掉，再对不上 MOPS。

B. **月值可加总 —— 实测，不是「功能货币是新台币」那种间接论证。**
   把 CSV 逐期加总，与公司已公布的经会计师查核（年）／核阅（季）数逐笔对：
     · FY2017 / FY2024 / FY2025 营业收入（年报的经营结果表）；
     · 2025Q1 / 2026Q1、2025H1 / 2026H1（核阅合并财报的综合损益表）。
   七期**合计口径全部逐位相等**；分部口径 12 项里 10 项逐位相等、最大差 1 千元
   （2026H1，官方自己在季度拆分上的进位差，本页照录不修）。
   对账在 `_audit_recon()` 里**构建期重跑**，结果写进页尾 —— 哪天 CSV 漂了，
   那句话会自己改口，而不是留一句过期的「已核对」。
   ⇒ `value.summable = True` 有实证撑着：季度桥、QTD/YTD、12 个月滚动同比三道加总合法。
   对照组是世芯-KY（3661）：同一种检验在那边差 +0.378%，因为它功能货币是美元、
   新台币月营收是逐月折算值 —— 那种家的 NT$ 列只能进 `alt`（`summable=False`）。

C. **官方逐月给业务拆分，这是 TSMC 月报里没有的一层，所以它进图（`segments`）。**
   同一个口径在三份官方文件里有三种叫法，本页统一用**月度 xlsx 的叫法**（数据就来自它）：
       月度 xlsx（本页数据源）   季报附注「收入拆解」      年报「营业收入分类」
       Turnkey                  Wafer product            ASIC & Wafers
       NRE / Others（≤2025）    NRE&IP                   NRE / Others
       NRE & Others（2026 起）
   映射关系是**对出来的**不是猜的：2025Q1、2026Q1 两期的月度加总，与季报附注的
   Wafer product / NRE&IP 逐位相等（见 `_REF`）。
   ⇒ 于是 2026-01 起 xlsx 把 `NRE` 与 `Others` 并成一行**不构成本页的口径断点**：
     本页那一列从 2017-01 起始终等于审计口径的 NRE&IP，落库时就已经把 ≤2025 的
     NRE + Others 合并掉了。这一条同样是对出来的，不是「应该没问题」。
   ⇒ **占比图上不标这个断点**（`breaks` 照旧为空）。判据不是「合并之后看着连续」，
     而是这三条：① 画出来的那个量（分部 ÷ 合并）在 115 个月里定义**逐月同一**，
     两侧都等于审计口径的 NRE&IP；② 分母根本碰不到这次改版 —— 落库不读 Total 行
     （口径坑 1），合计是两个分项之和，`Turnkey` 那一行也没动；③ 红虚线的语义是
     「线左线右不是一回事、不要跨着读」，在这里画就是一句假话，而且是**第二句**
     假话（还会顺带宣称本页存在两套口径）。真正会出假跳变的是「照四行建三列」
     那条没被采纳的路，那也不该用红虚线遮，落库合并才是修法。
   ⚠️ 但合并有代价，本轮把代价也写上页：2017-2025 那 108 个月官方本可拆成**三**块，
     本页只画两块，丢掉的是 `Others` 单列的月度粒度。CSV 里没有它，本页
     **复算不出来**（与 `_SKIP_FX_LINES` 里的地区占比同一档：页面只能指路）。
     取舍理由是这一刀砍在对的地方：留三列会让第三列在 2025-12 断掉、第二列在
     2026-01 改义 —— 那才是**真断点**，而且正好落在一张以「连续占比时序」为命题的
     图中间；而两列这一刀恰好就是季报附注 NRE&IP 的那一刀，是唯一逐期对得上账的
     切法。年度粒度的三分法并没有消失，仍在年报「营业收入分类」表里（`_REF` 的
     AR25 一行就是从那张表读的）。这一段进 `note_extra['mix']`，不留在注释里。

D. **有汇率线（`fx_rate`），没有美元腿（`fx_lines` / `fx_contrib` 显式 `skip`）——
   这是两件事，本轮把它们拆开了。**
   本页此前把 `fx` 整个留空，于是底座把三张图捆在一起跳掉。那对前两张是对的，
   对 `fx_rate` 是错的：**它画的是汇率本身**，一条宏观序列（`series/tsm_fx.csv`），
   挂同一份汇率的每一页上逐点相同，不需要本家披露任何东西。判据见底座 §1.5。

   那两张该跳的理由要说准，不是「公司没有外币敞口」——
   · 公司自己在合并财报「金融工具 · 市场风险 · 外币风险」一节写
     "operating activities are mainly denominated in foreign currency"（2026Q2 核阅本
     **Note 28 FINANCIAL INSTRUMENTS** → Market risk → Foreign currency risk，实读原文
     核过；⚠️ 不是 Note 30，那一条是 PLEDGED OR MORTGAGED ASSETS）；敞口是**有**的，
     而且给了敏感度分析（新台币升值 10% → 2026H1 税前净利减少 NT$374,871 千元）。
   · 但公司从不披露美元营收金额，也不披露以美元计价的**占比**：季报的收入拆解
     （Note 18b）按 Production / Region / Application Type 三个维度切，
     **没有一维是计价货币**（本轮逐维核过 2026Q2 那份）。
   · 于是 `NT$ ÷ 牌价` 折出来的那条「美元营收」暗含一个**100% 以美元计价**的假设。
     TSMC 那条腿之所以成立，是因为它每季自报美元营收、还用美元给指引，构造值有实绩
     可以对账、占比也有 20-F 可查；本家两样都没有 —— 真实占比未知，那条线因此
     **既对不了账、也定不了标**，`fx_contrib` 的「汇率贡献 pp」会按这个错误占比被整体放大。
   ⇒ 正确写法是 `fx` 照给 + `skip: ['fx_lines', 'fx_contrib']` + 两条 `skip_note`
     （`skip` 正是给「有腿也不画」用的）。理由逐条写在 `_SKIP_FX_LINES` /
     `_SKIP_FX_CONTRIB` 里，会印进页尾。
   ⚠️ 不要把季报的**地区**拆分当计价货币的代理（见 `_SKIP_FX_LINES`）。

E. **没有指引桥，`guidance` / `brief_extra` 都不给。**
   本家已于 2026-01-30 的 4Q25 法说会宣布停止提供数字财测；2026-07-30 的 2Q26 IR
   简报全文检索 guidance / outlook / forecast / next quarter **零命中**（唯一含
   "expect" 的一句是前瞻性陈述免责声明）。而且 `series/tsm_guidance.csv` 里的
   `guide_fx_ntd_per_usd` 一列对本家在**概念上**就不存在 —— 那是给「用美元指引、
   用新台币记账」的公司准备的。这不是数据缺失，是对象不存在。
   ⇒ 底座自动退回 brief 的替补句；本家有分部列，第 4 句会走「合并 ≡ 各分部之和」
     的构成句，那正是本页最该说的一句话。

F. **`official_yoy` 不给 —— 但原因不是「公司不公布同比」。**
   本家的**法定**月营收申报是带同比的：TWSE OpenAPI `t187ap05_L`（= MOPS 月营收彙總表）
   逐月给「营业收入-去年同月增减(%)」。核对最新一期：官方值与本页自算的单月同比在
   IEEE-754 双精度下**只差末位**（两者都是同样两个 NT$K 数相除减一，与 `build/yoy.py`
   同式）。`series/guc.csv` 没有这一列，所以底座只能走自算；两者算式同源，无口径损失。
   底座在没有 `official_yoy` 时只会陈述「本页 spec 未登记」这个**状态**，并明说
   「要说公司披露了什么，请由该家 spec 带出处来说」—— `_NOTE_OFFICIAL_YOY` 就是那句。
   要根治得在落库侧加一列 `yoy_pct`（`fetch/guc.py` 已经在读 t187ap05_L），
   属数据侧改动，不在本配置范围。

G. **序列起点 2017-01 是「本页数据源的起点」，不是「公司披露的起点」。**
   月度 Turnkey / NRE & Others 拆分只有 IR 的那份 xlsx 有，而它自 2017-01 起。
   合并月营收本身在 MOPS 上早得多（`t21sc03` 的 2016-01、2011-01 两期都查得到）。
   所以全历史图标题的 "Full monthly revenue history since 2017" 要读成
   「**本页口径**的全历史」。
   ⇒ `window.x_from` **显式写 None**：用序列自己的起点。不写 `'2016-01'` —— 本序列
     根本没有 2016 年，而且真把更早的合并营收接上来，会得到一段**没有分部拆分**的历史，
     两条分部线在那段要么断掉、要么被补成假值，两种都比「从 2017 起」更糟。
     （半导体组里序列晚于 2016 的另外两家 ASE / MTK 也是显式 None，同一个口径。）

H. **`breaks` 为空，`continuity` 给出处但自带适用范围。**
   2025 年度年报「因併購而發行新股：無」，关系企业一节列的全是全资自设子公司
   （GUC-NA / Japan / Korea / Europe / CN），全文搜不到 restatement；本页序列与 MOPS
   法定申报在 2017-01 → 2025-12 之间抽查逐位相等，两个独立官方源对得上。
   但那份年报覆盖的是**最近年度**，更早年份的合并范围要自己回溯各年年报 ——
   这句限定写进 `continuity.zh` 本身，不让读者以为出处比它实际覆盖的更宽。

━━ 落库口径（`fetch/guc.py` 已实现，这里只记为什么）━━━━━━━━━━━━━━━━━━━━
xlsx 的 `Total` 行有大量单元格是**活公式**（实测 115 格中 61 格），`openpyxl` 的
`data_only=True` 读到的是 Excel 写入的**缓存**—— 缓存是发布者保存工具的副产品，
上游哪个月换用别的工具另存就整块变 None、静默漏年。
⇒ 一律对两个分项求和，不读 Total 行。重抓官方文件逐格复核：115 个月的分项和与
  Total 行无一例外相等，且与 `series/guc.csv` 的三列逐位相等。

━━ 哪些数是转录的（不能由 CSV 复算，随年报／法说会复核）━━━━━━━━━━━━━━━
本文件里带数字的图注**全部构建期现算**，读不到源退回定性版本、不抛异常。真正转录的
只有两处，各自就地用 ⚠️ 标出：
  · `_REF` 里的七期查核／核阅数（外部基准，见该表上方的说明）；
  · `_SKIP_FX_LINES` 里收入拆解附注的两个**地区**占比（本页不能复算 —— CSV 里只有
    月营收与两条分部线，没有地区维）。⚠️ 原来这里写的出处是 `build/specs/guc.py`，
    那个文件已在 4b0f201 删掉，指过去是条死链。现按**一手源**记：GUC 2025 年度合并
    财务报告（会计师查核）附注 17b「收入拆解 → Region」，United States 一行
    2025 = 23,207,257／合计 34,140,978 = 68.0%，2024 = 7,995,051／合计
    25,044,192 = 31.9% —— 两个数逐位复算过，与页上印的一致。同一张表列的是
    United States／China／Taiwan／Japan／Korea／Europe 六个**销售地区**，
    与 2026Q2 核阅本 Note 18b 的三维切法同源，**都没有一维是计价货币**。
`fx.usd_share_note` 则**一个数字都不带**：本家没有任何可引的计价货币占比表述，
底座允许的退路正是「定性事实 + 可点开的出处」，不许拿别家的百分比来充数。
"""
import csv
import os

from . import _facts

_CSV = 'guc.csv'
_COL = 'revenue_ntd_mn'
_TURNKEY = 'revenue_turnkey_ntd_mn'
_NRE_COL = 'revenue_nre_other_ntd_mn'

# 核对表印几行。写成常量是因为图注里要引「那一列覆盖的就是这 N 个月」——
# 两处一旦分叉，页面会指着一列 13 行的表说「上面 24 个月里…」。
_CHECK_ROWS = 13

# GUC IR 财务资讯页。月营收 xlsx 与财报都挂在这里；xlsx 的直链每月随上传日改一次，
# 所以出处一律写落地页，不写那个会过期的直链（同 fetch/guc.py 口径坑 3）。
_IR = 'https://www.guc-asic.com/en/investor/financial'


# ══════════════════════════════════════════════════════════════════════════════
# 构建期现算：图注/页尾说明里的数**一个都不写死**。
# 读不到源就退回不含数字的定性版本（每个函数拿不到都返回 None，import 期不抛异常 ——
# 一份 spec 是被 import 进来的，为了「少一句话」把整批构建打死不划算）。
# ══════════════════════════════════════════════════════════════════════════════
def _rows():
    try:
        with open(os.path.join(_facts.SERIES, _CSV), encoding='utf-8') as fh:
            return list(csv.DictReader(fh))
    except Exception:
        return None


def _fv(v):
    try:
        return float(v)
    except (TypeError, ValueError):
        return None


# ── 对账用的**参照数**：公司已公布的查核／核阅数，单位 NT$K。 ────────────────
#
#   这不是「图注里的数」，是**外部基准**：它们是已定稿的历史事实（年报/季报上的印刷体），
#   不会随本页数据更新而变化，所以写在这里是安全的；真正会随数据变的那一侧
#   （CSV 的逐期加总、两者的差额）在 `_audit_recon()` 里构建期现算。
#   这一段的价值正在于「会自己失效」：CSV 哪天被改坏，页尾那句话会立刻从
#   「逐位相等」变成带差额的措辞，而不是继续印一句过期的保证。
#
#   ⚠️ **每年出新年报要补一行并回头复核**，不要只往下加。选期覆盖序列两端：
#      最早的完整年（FY2017）+ 最近两个完整年 + 最近两组季/半年。
#
#   出处（落地页均为 _IR）：
#     [AR17]  2017 年度年报 · 经营结果（合计口径；该年无逐分部的审计拆分参照数）
#     [AR25]  2025 年度年报 · 财务状况及经营结果之检讨分析 · 2. Operating Results；
#             同份年报「营业收入分类」表给 ASIC&Wafers / NRE / Others 三行
#     [Q1-26] 2026 年第一季合并财报（会计师核阅）· 综合损益表 + 收入拆解附注
#     [H1-26] 2026 年上半年度合并财报（会计师核阅）· 综合损益表 + 收入拆解附注
_REF = [
    # (标签, 起月, 迄月, 合计, Turnkey, NRE&Others, 出处)
    ('FY2017',  '2017-01', '2017-12', 12_160_606,       None,      None, 'AR17'),
    ('FY2024',  '2024-01', '2024-12', 25_044_192, 16_161_027, 8_883_165, 'AR25'),
    ('FY2025',  '2025-01', '2025-12', 34_140_978, 25_735_801, 8_405_177, 'AR25'),
    ('2025Q1',  '2025-01', '2025-03',  7_023_644,  5_665_268, 1_358_376, 'Q1-26'),
    ('2026Q1',  '2026-01', '2026-03', 11_447_812,  9_805_073, 1_642_739, 'Q1-26'),
    ('2025H1',  '2025-01', '2025-06', 13_128_221,  9_688_693, 3_439_528, 'H1-26'),
    ('2026H1',  '2026-01', '2026-06', 25_344_369, 21_391_160, 3_953_209, 'H1-26'),
]


def _audit_recon():
    """把 CSV 逐期加总，与 `_REF` 的查核／核阅数对账。拿不到返回 None。

    分两组报：**合计**一组、**分部**一组。分开报是必要的 —— 合计七期全部逐位相等，
    而分部有一期差 1 千元（官方自己在季度拆分上的进位差）。
    合成一个数会把「合计完全对得上」这条最强的证据稀释掉。
    """
    rs = _rows()
    if not rs:
        return None
    by = {}
    for r in rs:
        t = _fv(r.get(_COL))
        k = _fv(r.get(_TURNKEY))
        n = _fv(r.get(_NRE_COL))
        if t is None:
            continue
        by[r['month']] = (t * 1000.0, None if k is None else k * 1000.0,
                          None if n is None else n * 1000.0)
    tot = {'n': 0, 'exact': 0, 'maxabs': 0.0, 'worst': None}
    seg = {'n': 0, 'exact': 0, 'maxabs': 0.0, 'worst': None}
    for lab, m0, m1, rtot, rtk, rnre, _src in _REF:
        ms = [m for m in by if m0 <= m <= m1]
        want = (int(m1[:4]) - int(m0[:4])) * 12 + int(m1[5:7]) - int(m0[5:7]) + 1
        if len(ms) != want:                     # 期间没配齐就整条跳过，不报半个结论
            continue
        s_t = sum(by[m][0] for m in ms)
        for box, mine, ref in ((tot, s_t, rtot),
                               (seg, sum(by[m][1] for m in ms) if all(
                                   by[m][1] is not None for m in ms) else None, rtk),
                               (seg, sum(by[m][2] for m in ms) if all(
                                   by[m][2] is not None for m in ms) else None, rnre)):
            if mine is None or ref is None:     # 该期没有这一栏的审计参照数 ⇒ 不报
                continue
            d = abs(mine - ref)
            box['n'] += 1
            if d < 0.5:
                box['exact'] += 1
            elif d > box['maxabs']:
                box['maxabs'], box['worst'] = d, lab
    if not tot['n']:
        return None
    return {'tot': tot, 'seg': seg, 'periods': tot['n']}


def _seg_additivity():
    """逐月「Turnkey + NRE&Others 是否恰好等于合计」。拿不到返回 None。

    页面在 brief 第 4 句和页尾都会说「合并 ≡ 各分部之和」。那是一句**恒等式断言**，
    所以它必须在构建期被这份 CSV 自己验一遍，而不是引用一句读来的话。
    """
    rs = _rows()
    if not rs:
        return None
    n, worst, wm = 0, 0.0, None
    for r in rs:
        t = _fv(r.get(_COL))
        k = _fv(r.get(_TURNKEY))
        v = _fv(r.get(_NRE_COL))
        if None in (t, k, v):
            continue
        n += 1
        d = abs((k + v) - t) * 1000.0            # NT$K
        if d > worst:
            worst, wm = d, r['month']
    return {'n': n, 'worst_ntdk': worst, 'worst_month': wm} if n else None


def _span():
    """(首月, 末月, 月数)。拿不到返回 (None, None, 0)。"""
    rs = _rows() or []
    ms = [r['month'] for r in rs if _fv(r.get(_COL)) is not None]
    return (ms[0], ms[-1], len(ms)) if ms else (None, None, 0)


def _remark_seg():
    """核对表那一列的 MOPS 备注原文里，公司自己怎么点分部的名。拿不到返回 None。

    为什么本页要算这个：堆叠柱把 `NRE & Others` 画成**一整块**，视觉上暗示它是一项
    单一业务。反证不必外求 —— 就在同一页的核对表里：公司在备注栏里把「晶圓產品」
    与「委託設計(NRE)」当两件事分开说，而「其他」一次都没被点过名。
    这是**页内可逐格复核**的证据（读者对着那一列数得出来），所以它比任何转录的
    断言都硬；也因此它必须现算 —— 备注逐月在变，写死的计数下个月就是假的。

    只统计核对表**实际印出来的**那 `_CHECK_ROWS` 个月，不是全表 24 行：
    图注说「上面那一列」，指的就得是上面那一列。
    `series/mops_remarks.csv` 是别人维护的文件，读不到/列变了一律返回 None，
    那一句话整句消失，不带半个数上页。
    """
    ms = [r['month'] for r in (_rows() or []) if _fv(r.get(_COL)) is not None]
    if not ms:
        return None
    want = set(ms[-_CHECK_ROWS:])
    try:
        with open(os.path.join(_facts.SERIES, 'mops_remarks.csv'),
                  encoding='utf-8') as fh:
            rk = [r for r in csv.DictReader(fh)
                  if r.get('ticker') == 'guc' and r.get('month') in want]
    except Exception:
        return None
    if not rk:
        return None
    txt = [(r.get('remark') or '').strip() for r in rk]
    filled = [t for t in txt if t]
    if not filled:
        return None
    return {'shown': len(want), 'rows': len(rk), 'filled': len(filled),
            # 繁体原文里的写法：委託設計 / NRE 指同一件事，其他 = Others。
            'nre': sum(1 for t in filled if '委託設計' in t or 'NRE' in t),
            'others': sum(1 for t in filled if '其他' in t or 'Others' in t),
            'variants': len(set(filled))}


_RECON = _audit_recon()
_ADD = _seg_additivity()
_M0, _M1, _MN = _span()
_NRE = _facts.share_range(_CSV, _NRE_COL, _COL)
_RK = _remark_seg()
# `_facts.yoy_extremes()` 不再取用：色阶拥挤度改由底座在矩阵图注里按**本表实际用的那张
# matrix** 现算（见 `_NOTE_HEAT` 上方那段 ⚠️）。全序列分位与引擎看的分位不是同一个东西。
_DAYS = _facts.days_effect(_CSV, _COL)


# ── 页尾说明 ──────────────────────────────────────────────────────────────────
_NOTE_SOURCE = (
    '<b>数据源与落库口径。</b>历史取 GUC 投资人关系页「Historical Monthly revenue」'
    '官方 xlsx（单 sheet <code>revenue breakdown</code>，单位 NT$K），'
    '并与 MOPS 月营收彙總表（<code>t21sc03</code>）、TWSE OpenAPI '
    '（<code>t187ap05_L</code>）交叉核对 —— 在 2017-01 至 2025-12 之间抽了六个月'
    '逐位对，两个独立官方源全部相等。'
    '⚠️ 该 xlsx 的 <code>Total</code> 行有大量单元格是<b>活公式</b>而非字面值，'
    '<code>openpyxl</code> 的 <code>data_only=True</code> 读到的是 Excel 写入的缓存，'
    '而缓存是发布者保存工具的副产品 —— 上游哪个月换个工具另存就整块变 None、静默漏年。'
    '所以落库一律<b>对两个分项求和</b>，不读 Total 行；'
    '重抓官方文件逐格复核，全部月份的分项和与 Total 行无一例外相等。'
    '另外该文件的下载 URL 每月一换（路径含上传日），因此链接每次从落地页现抓、不写死，'
    f'本页出处一律给落地页 {_IR} 。'
    '公告节奏：台湾《证券交易法》要求次月 10 日前公告，本家惯例是次月 5 日，'
    '撞假日顺延（不要拿「次月 5 日」外推，每次去新闻稿现读）。'
    # ⚠️ 这里**不要逐字引用**页尾「数据源」那条的措辞。它原来引的是「由这一个字段…
    #    派生」，而底座早已把那句改成现算的字段清单（有分部列的家印的是「这 3 个官方
    #    披露字段（合并、量产、委托设计与其他）」）—— 页面于是出现一句引号里的话，
    #    在同一页上一个字都查不到。改成描述它**做了什么**，措辞变了也不会变成假引文。
    '<b>⚠️ 上面「数据源」那条把本页的官方披露字段逐个列了出来'
    '（月均汇率那张图是它自己点名的例外，来源另写在该图图脚），'
    '那份清单在本家要这样读</b>：真正独立的只有<b>两个分项</b>'
    '（Turnkey 与 NRE &amp; Others）—— 合计是它们的和，不是第三个来源：'
    '落库根本没读官方 xlsx 的 Total 行（理由见上），三者之间没有第二套口径。'
    + ((f'构建期逐月复算：{_ADD["n"]} 个月里'
        + ('<b>合并恰好等于两个分部之和</b>（最大偏差 0 千元）。'
           if _ADD['worst_ntdk'] < 0.5 else
           f'最大偏差 {_ADD["worst_ntdk"]:,.0f} 千元（{_ADD["worst_month"]}）。'))
       if _ADD else ''))

_NOTE_ADDITIVE = (
    '<b>本页的季度聚合、QTD／YTD 与 12 个月滚动同比都是合法的 —— 这一条在构建期实测，'
    '不是推定。</b>'
    + ((f'把 <code>series/{_CSV}</code> 逐期加总，与公司已公布的查核／核阅数对账：'
        f'合计口径 {_RECON["tot"]["n"]} 期里 <b>{_RECON["tot"]["exact"]} 期逐位相等</b>'
        + ('（无差额）' if _RECON['tot']['exact'] == _RECON['tot']['n'] else
           f'，最大差 {_RECON["tot"]["maxabs"]:,.0f} 千元（{_RECON["tot"]["worst"]}）')
        + f'；分部口径 {_RECON["seg"]["n"]} 项里 {_RECON["seg"]["exact"]} 项逐位相等'
        + ('（无差额）。' if _RECON['seg']['exact'] == _RECON['seg']['n'] else
           f'，最大差 {_RECON["seg"]["maxabs"]:,.0f} 千元（{_RECON["seg"]["worst"]}，'
           '官方自己在季度拆分上的进位差，本页照录不修）。')
        + '参照期覆盖序列两端：最早的完整年（2017）、最近两个完整年（2024／2025），'
          '以及 2025／2026 两组第一季与上半年。'
          '参照数取各年年报的经营结果表与季／半年度合并财报（会计师核阅）的'
          '综合损益表与收入拆解附注，'
        + _IR + ' 。')
       if _RECON else
       '（本轮未能从 CSV 现算出对账结果，故此处只作定性表述：'
       '月营收以新台币原生记账，逐期加总与公司公布的查核／核阅营业收入同口径。）')
    + '⚠️ 这句话会随数据自己改口：CSV 哪天被改坏，上面的「逐位相等」会立刻变成带差额的'
      '措辞，而不是继续印一句过期的保证。'
      'GUC 的功能货币与表达货币都是新台币，月值是原生记账数、不是折算值，所以加得起来。'
      '对照组是同业世芯-KY（3661）—— 那家功能货币是美元、新台币月营收是逐月折算值，'
      '同一种检验会差出千分之几（+0.378%），它的 NT$ 列在本底座里只能进 '
      '<code>alt</code>（<code>summable=False</code>）、不许当 <code>value</code>，'
      '在结构上进不了季度桥、YTD 与滚动同比。')

_NOTE_PRECISION = (
    '<b>单位与精度：官方原始单位是新台币千元（NT$K），本页一位都没丢。</b>'
    'IR 那份 xlsx 的表头逐字是 <code>Unit: NT$K</code>；'
    f'<code>series/{_CSV}</code> 的三列虽然叫 <code>*_ntd_mn</code>，'
    '但每一格都带 3 位小数，是官方千元数除以 1000 的原位搬运。'
    '因此核对表印到<b>小数点后三位</b>：那三位不是估计出来的精度，'
    '少印一位就是把官方的千元位四舍五入掉，再拿去与 MOPS 对就对不上了。'
    '各图与汇总表按整数百万（NT$mn）显示，那只是显示取舍，参与计算的一直是全精度值。'
    '<b>显示单位取 NT$mn 而不是 NT$bn，是跑过反事实的</b>：本家月营收在 0.7 ~ 5.8 NT$bn '
    '之间，底座给月营收柱的柱值标签写死整数格式（<code>f0</code>），换成 bn 之后 2017 年'
    '那批柱会印成「1」、最新月印「6」，整张图只剩个位数六个值 —— 那是拿三位有效数字'
    '换一个更整洁的单位，不划算。')

# ⚠️ 本条**不再自己报占比的极值**。分部占比那张图（`mix`）本轮上线之后，底座已经在
#    它自己的图注里印出两条线的最新值与**窗口内**两端极值（带月份）。本文件原来印的是
#    `_facts.share_range()` 的**全序列**极值：今天两者恰好同格（`x_from` 显式为 None，
#    窗口就是全序列），但那是巧合级的同格 —— 哪天窗口一截，页上就会出现两份打架的数。
#    这与 `_NOTE_HEAT` 上面那段是同一个教训，本轮一并处理。
#    留在这里的只有底座讲不了的两层：**为什么不平滑**，以及**尖刺该去哪张图上归因**。
_NOTE_MIX = (
    # ⚠️ 抬头本轮改过。旧版是「月营收的 lumpy 来自 NRE」—— 而本轮页上另有两段
    #    （note_extra 的 rev_bar / mix）白纸黑字说「NRE 与 Others 并成一格，本页分不出来」。
    #    把 lumpy 整个记到 NRE 头上，正是那两段在劝读者别做的那个归因，同一页自相矛盾。
    #    能证明的只到「那一格」为止：它的占比在页上摆得极宽，而 Turnkey 那一格没有。
    '<b>月营收的 lumpy 来自 NRE &amp; Others 那一格，不是噪声 —— '
    '所以本页把官方拆分画进图里，而不是给曲线加平滑。</b>'
    + ((f'全序列 {_NRE["n"]} 个月排下来，NRE &amp; Others 占当月营收的<b>中位</b>是 '
        f'<b>{_NRE["med"]:.1f}%</b>，而最新月只有 {_NRE["cur"]:.1f}% —— '
        '今天这个占比按它自己的历史看偏低，不是常态。'
        '两端极值与逐月轨迹在分部占比那张图上（本条不重复：那张图报的是<b>窗口内</b>'
        '极值，本页 <code>x_from</code> 显式为 None、窗口就是全序列，'
        '两个口径今天同格，但只该有一处印数）。')
       if _NRE else
       'NRE（委托设计）按里程碑确认，占当月营收的比重逐月摆动很大。')
    + '同一家公司，某些月份大半营收落在 NRE &amp; Others 那一格里、某些月份几乎全是量产晶圆。'
      '那一格为什么会这么摆：里头的 NRE 是按<b>里程碑</b>确认的一次性工程费，'
      '一笔里程碑落在哪个月，那个月就鼓一块。'
      '⚠️ 但只能说到<b>这一格</b>为止 —— 同一格里还有 Others，本页分不开'
      '（见月营收柱与分部占比两张图的图注）。'
    # ⚠️ 指路牌本轮改过。旧版写的是「先看全历史图上那两条分部线」——
    #    那在 `rev_bar` 还没按分部堆叠的年代是页上唯一的分部视图，现在有两处问题：
    #    ① 月营收柱本身已经分色，归因一眼就能做；② 全历史图自己的图注白纸黑字写着
    #    「看得出的是量级台阶而不是月度噪音；月度波动请看月营收柱」—— 旧版正好把读者
    #    支到那张图上去读单月尖刺，与它自己的图注对着干。
    + '<b>读环比与单月同比之前，先看月营收柱上那两段颜色</b>：尖刺通常能直接归到 '
      'NRE &amp; Others 那一段，柱高一格没变、只是填色按业务分块。'
      '<b>不要去全历史图上找尖刺</b> —— 那张图纵轴自 0 起、横跨全部月份，'
      '它自己的图注就写着「月度波动请看月营收柱」，本条与它同解。'
      '这也是本页<b>不给月营收加任何平滑</b>的原因：平滑会把这层可归因的结构抹成噪声。'
    # 三种叫法的对照表搬去了占比图自己的图注（note_extra['mix']）：底座在那张图上
    # 明说「分部列各自是什么……见本注下方各家自己的说明」，读者是在那里问的，
    # 答案就该在那里。留在这里会变成第二份说法，两份迟早分叉。
    + '官方三份文件对同一个口径有三种叫法，对照表写在分部占比那张图的图注里。')

_NOTE_SEG_BREAK = (
    '<b>2026-01 起官方把 <code>NRE</code> 与 <code>Others</code> 并成一行，'
    '但这不构成本页的口径断点</b>（所以本页 <code>breaks</code> 为空，图上没有红虚线）。'
    'xlsx 的年份块在 2017–2025 是 <code>Turnkey / NRE / Others / Total</code> 四行，'
    '2026 起变成 <code>Turnkey / NRE &amp; Others / Total</code> 三行。'
    '若照四行建三列，2026 起会一列全空、一列偏高 —— <b>本页现在正好有一张分部占比'
    '时序图</b>，那个<b>假跳变</b>会结结实实地画在它中间，不再是假想的后果。'
    '本序列因此只存两列，2017–2025 的 NRE + Others 在落库时就合并掉了 ——'
    '真断点在落库那一层被抹平，而不是在图上用一条红虚线遮过去。'
    '合并之后那一列是否真的与 2026 年那一行同口径，是<b>对出来的</b>：'
    '2025Q1 与 2026Q1 两期的月度加总与季报附注的 NRE&amp;IP 逐位相等 —— '
    '两边都等于同一个审计口径，所以这一列自 2017-01 起逐月连续。'
    '<b>这一条对读图的影响、以及合并掉 Others 单列月度粒度的代价，'
    '写在分部占比那张图自己的图注里</b>：读者是在那张图上问「2026-01 这里是不是断了」的，'
    '答案就该挂在那里，本条只留落库这一层的记录。')

# ══════════════════════════════════════════════════════════════════════════════
# 逐图补注（`note_extra`）—— 追加到那一张图自己的图注末尾。
#
# 为什么这两段必须在这里、不能留在页尾：
#   · `mix`：底座在那张图的图注里只说「占比是现算的：分部 ÷ 合并」，然后<b>明说</b>
#     「分部列各自是什么、哪一列是官方披露值哪一列是残差，见本注下方各家自己的说明」。
#     底座读不出这一层（是不是残差是 per-ticker 事实），它不兜底 —— 本页不写，
#     那张图上就只剩一句「见下方说明」而下方什么都没有。
#   · `rev_bar`：堆叠柱把 `NRE & Others` 画成一整块连续色，视觉上断言它是一项业务。
#     底座讲不了这件事（它不认得本家的 NRE），而这个暗示恰恰是本页最容易被读错的地方。
# ══════════════════════════════════════════════════════════════════════════════
# ⚠️ 段的上下位置**别凭印象写**：`assets/charts.js` 的 gs_bar 堆叠从 `sbase = 0` 起
#    逐段往上摞，所以 `segments[0]`（Turnkey）在柱底、`segments[1]`（NRE & Others）
#    在柱顶 —— 与底座那句「柱顶那个数是合计，不是最上面那一段」正好接得上。
#    本条初稿把上下写反了，verify_pages 全绿、页面照印，只有对着图看才发现。
_NX_REV_BAR = (
    '<b>⚠️ 最上面那一段（NRE &amp; Others）是两类收入并成的一整块，不是一项业务。</b>'
    '堆叠柱的视觉语法会把一段连续的颜色读成一条业务线，而这一段里同时装着 '
    'NRE（委托设计的一次性工程费，按里程碑确认）与 Others 两种完全不同的收入。'
    '所以这一段变厚<b>不等于</b>委托设计接单变多 —— 页面没有能力把两者分开，'
    '2026-01 起官方自己也不再分（沿革与代价见分部占比那张图的图注）。'
    '柱底那一段的 Turnkey 则是单一口径（＝ 季报附注的 Wafer product '
    '＝ 年报的 ASIC &amp; Wafers）。'
    '<b>两段的高度也不是同一种波动</b>：Turnkey 段跟量产出货走，'
    'NRE &amp; Others 段按里程碑确认 —— 一笔里程碑就能把它顶起来，'
    '所以先读柱高（合并），再读分段（结构），别拿分段的月度起伏直接推经营趋势。')

_NX_MIX = (
    '<b>本页这两列都出自公司的官方披露，没有一列是残差</b>'
    '（残差 ＝ 合并 − 某一块；本站别的月营收页上有这种列，本页<b>没有</b>）。'
    '数据源是 GUC IR 那份月度 xlsx 的 <code>revenue breakdown</code> sheet：'
    'Turnkey 是表上的一行、原位搬运；NRE &amp; Others 自 2026-01 起也是表上的一行，'
    '2017–2025 则是官方 <code>NRE</code> 与 <code>Others</code> 两行<b>相加</b> —— '
    '加法在落库时做，两个加数都是官方数，没有一格是我们倒挤出来的。'
    '合计同样不是第三个来源，是这两列相加（落库<b>不读</b>该表的 <code>Total</code> 行，'
    '理由见页尾「数据源与落库口径」）。所以本图的分子与分母都出自同一张表，'
    '不含任何我们自己的分摊或估计。'
    '<b>同一个口径在官方三份文件里有三种叫法</b>，本页统一用月度 xlsx 的叫法'
    '（数据就来自它）：Turnkey ＝ 季报附注的 Wafer product ＝ 年报的 ASIC &amp; Wafers；'
    'NRE &amp; Others ＝ 季报附注的 NRE&amp;IP ＝ 年报的 NRE ＋ Others。'
    '这组映射是把月度加总与季报附注对出来的，不是按名字猜的（对账见页尾）。'
    # ── 与 rev_bar 那条同一件事，但这里必须再说一次：这张图上它是一条**线**，
    #    读者看到的是一条平滑走势，比堆叠柱更像「一项业务的份额」。
    + '<b>⚠️ 其中 NRE &amp; Others 那条线是两类收入并成的一格</b>：'
      'NRE 是委托设计的一次性工程费（按里程碑确认），Others 是另一类收入，'
      '2026-01 之前官方分两行给、本页只画一条。'
      '所以这条线抬头，可能来自 NRE 也可能来自 Others，<b>本页分不出来</b>，'
      '读者也不该替它分。'
    + ((f'旁证就在本页核对表的「本月備註（MOPS 原文）」那一列：'
        f'印出来的 {_RK["shown"]} 个月里有 {_RK["filled"]} 个月填了原文，'
        f'公司自己把「晶圓產品」与「委託設計(NRE)」当两件事分开写'
        + (f'（{_RK["nre"]} 个月点了委托设计的名）' if _RK['nre'] else '')
        + ('，而「其他／Others」一次都没被点过名 —— '
           '并成一格是<b>披露格式</b>的合并，不是公司认为它们是一回事。'
           if not _RK['others'] else
           f'，其中 {_RK["others"]} 个月提到「其他」。'))
       if _RK else '')
    + '<b>2026-01 起官方把 NRE 与 Others 并成一行，但本图上没有断点标记 ——'
      '这是判断，不是漏了。</b>三条理由：'
      '① 本图画的那个量（分部 ÷ 合并）在全部月份里定义<b>同一</b>，'
      '两侧都等于季报附注 NRE&amp;IP 那个审计口径；'
      '② 分母碰不到这次改版 —— 合计是两个分项之和，<code>Turnkey</code> 那一行也没动，'
      '所以分子分母都不受影响；'
      '③ 红色竖虚线的意思是「线左线右不是一回事、不要跨着读」，在这里画就是一句假话，'
      '还会顺带宣称本页存在两套口径。'
      '真会画出假跳变的是「照四行建三列」那条<b>没被采纳</b>的路，'
      '而它的修法也不是红虚线，是落库时就合并（做法与对账见页尾）。'
    + '<b>代价一并写清楚：2017–2025 官方本可拆成三块，本图只画两块。</b>'
      '丢掉的是 <code>Others</code> 单列的<b>月度</b>粒度 —— '
      'CSV 里没有这一列，本页<b>复算不出来</b>（与页尾那两个地区占比同一档：'
      '页面只能指路，不能算）。'
      '想看那一层要回原始 xlsx 的 2017–2025 年份块，'
      '或年报的「营业收入分类」表（那里按年给 ASIC &amp; Wafers / NRE / Others 三行）。'
      '这一刀砍在这里，是因为它正好是审计口径的那一刀：留三列会让第三列在 2025-12 断掉、'
      '第二列在 2026-01 改义 —— 那才是真断点，而且正好落在这张以<b>连续占比</b>为命题的'
      '图中间，一条红虚线遮不住。')


# ── 两张美元腿图的 skip 理由（进页尾「本页不出「…」那张图」两条）───────────────
#   ⚠️ 这两条替代了旧版的 `_NOTE_NO_USD`。旧版写在 `notes` 里，说的是「本页没有美元腿」；
#      现在页上多了一条汇率线（`fx_rate`），底座会在页尾自己讲清「有汇率线、没有美元腿」这件事，
#      本页要补的是**逐图**的理由 —— 读者是在「为什么少了这两张」处问的，答案就该挂在
#      那两个 slug 上，而不是散在一条通用说明里。
_SKIP_FX_LINES = (
    '那张图要<b>两条腿</b>：本币营收与美元营收，而本家没有官方美元数。'
    '公司在合并财报「金融工具 · 市场风险 · 外币风险」一节写明营运活动主要以外币计价，'
    '还给了新台币升值 10% 的敏感度分析 —— 敞口是<b>有</b>的；'
    '但它从不披露美元营收<b>金额</b>，也不披露以美元计价的<b>占比</b>：'
    '季报的收入拆解按 Production／Region／Application Type 三个维度切，'
    '<b>没有一维是计价货币</b>。'
    '于是把新台币除以外部牌价折出来的那条线，暗含一个「营收 100% 以美元计价」的假设，'
    '而真实占比公司没说 —— 那条线既<b>没有官方数可以对账</b>、也<b>定不出正确的高度</b>，'
    '是分析师构造值。与本站 <code>/tsm/</code> 页不同：那边公司每季自报美元营收、'
    '还用美元给指引，构造腿有实绩可以对表，计价占比也有 20-F 可查。'
    '⚠️ 也不要拿季报的地区拆分（United States／Taiwan／China／Japan／Korea／Europe）'
    '当计价货币的代理：公司写明那是按<b>销售地区</b>归类，而它的美国<b>地区</b>占比'
    '2024 年 31.9%、2025 年 68.0% —— 一年之内走出三十多个百分点，'
    '这个数本身就说明它当不了计价货币的代理。'
    '（末尾这两个占比转录自 GUC 合并财务报告的收入拆解附注，本页不能复算，随年报复核。）')

_SKIP_FX_CONTRIB = (
    '它画的是「汇率贡献 ＝ 本币 y/y − 美元 y/y」，而上面那条美元腿在本家是构造值，'
    '所以这张跟着一起不出。'
    '这不是「少画一张」而已：在没有官方美元数的家身上，那根柱恒等于'
    '<code>汇率同比 ×（1 + 美元同比）</code> —— 完全由那条<b>宏观</b>汇率与本家自己的'
    '新台币增速决定，唯一能让它带上本家信息的那个量（真实的美元计价占比）'
    '恰恰是公司没披露的那个。按 100% 计价折出来，柱高会被整体放大到一个未知的倍数，'
    '读者却会把它读成「汇率替本家贡献了几个百分点」。'
    '汇率本身该看 —— 它在本页的月均 NTD/USD 那张图上，画的是<b>牌价本身</b>，'
    '不冒充本家的任何营收量。')

_NOTE_NO_GUIDANCE = (
    '<b>本页没有指引桥，也不该有。</b><code>/tsm/</code> 页顶那句「距指引中值还差多少」'
    '依赖三样东西：公司给的美元营收区间上下沿、公司假设的折算汇率、以及美元实绩。'
    '本家已于 2026-01-30 的 4Q25 法说会宣布<b>停止提供数字财测</b>；'
    '2026-07-30 的 2Q26 IR 简报全文检索 guidance／outlook／forecast／next quarter '
    '<b>零命中</b>（唯一含 "expect" 的一句是前瞻性陈述免责声明）。'
    '其中「假设汇率」一列在概念上都不存在 —— TSMC 需要它是因为用美元指引、用新台币记账，'
    '本家记账货币就是新台币，没有这个对象。这不是数据缺失，是对象不存在，'
    '所以本页不建 guidance 序列、也不拼那类句子，'
    'brief 自动退回「三月均值同比 vs 单月同比」的趋势位置句。'
    '（法说会那条转录自公司公开说明，随下一次法说会复核。）')

_NOTE_OFFICIAL_YOY = (
    '<b>关于上面「单月 y/y 全部由本脚本自算 / 本页 spec 未登记 <code>official_yoy</code>」'
    '那一条——底座只能陈述状态，'
    '公司披露了什么得由本页带出处来说，这里就说：本家的法定申报是<b>带</b>同比的。</b>'
    'TWSE OpenAPI <code>t187ap05_L</code>（＝ MOPS 月营收彙總表）逐月给'
    '「营业收入-去年同月增减(%)」与「上月比较增减(%)」。'
    '取最新一期与本页自算值对过：两者只差 IEEE-754 双精度的末位 —— '
    '因为官方那个百分比就是同样两个千元数相除减一，与 <code>build/yoy.py</code> 同式。'
    '所以本页走自算<b>没有口径损失</b>，'
    '但也不要从「本页没登记」反推出「公司不报同比」那个错误印象。'
    '要把官方原值搬进来，得在落库侧加一列 <code>yoy_pct</code>'
    '（<code>fetch/guc.py</code> 已经在读 t187ap05_L），那属于数据侧改动，'
    '不在本配置的范围内。出处：https://openapi.twse.com.tw/v1/opendata/t187ap05_L 。')

_NOTE_START = (
    '<b>本页的历史自 2017-01 起，那是「数据源」的起点，不是公司披露的起点。</b>'
    '月度 Turnkey／NRE &amp; Others 拆分只有 IR 的那份 xlsx 有，而它自 2017-01 起；'
    '合并月营收本身在 MOPS 上早得多 —— <code>t21sc03</code> 的 2016-01、'
    '2011-01 两期都查得到本家的当月营收。'
    '所以全历史图标题里的 "Full … history since 2017" 要读成「<b>本页口径</b>的全历史」，'
    '不要反推出「公司 2017 年才开始公布月营收」。'
    '本页宁可短一点也不混口径：把 2017 年之前的合并营收接上来，会得到一段没有分部拆分的'
    '历史，两条分部线在那段要么断掉、要么被补成假值 —— 两种都比「从 2017 起」更糟。'
    '窗口因此在 spec 里<b>显式写成「用序列自己的起点」</b>（<code>x_from = None</code>），'
    '而不是像 /tsm/ 那样卡一个 2016-01：本序列根本没有 2016 年，'
    '砍短则会与副标题的「共 N 个月」自相矛盾。'
    + (f'（副标题的覆盖范围 {_M0} – {_M1}、共 {_MN} 个月，说的是同一件事。）'
       if _M0 else ''))

# ⚠️ 本条**不再自己报色阶拥挤度的数**。底座已经在那张矩阵自己的图注里现算并印出
#    （5/95 分位、最宽 20% 色带塞了几格、三个口径的横向对照），而且它算的是**本表实际
#    用的那张 matrix**（`heat_years` 截出来的那几年 × 12 列）—— 引擎的分位也只看 matrix。
#    本文件原来引的是 `_facts.yoy_extremes()` 的**全序列**分位：今天两者恰好同格
#    （序列自 2017-01 起、同比自 2018-01 起，正好 9 个年度都进了矩阵），但窗口一滚
#    就会对不上，页上会出现两份打架的数。留在这里的只有「口径为什么这么选」这一层
#    ——那一层底座讲不了，它不认得本家的 NRE。
_NOTE_HEAT = (
    '<b>热力矩阵为什么留在本页、又该怎么读。</b>'
    # 这里原来自己复述一遍「某个月三分之二营收来自委托设计、下个月几乎全是量产晶圆」，
    # 与 _NOTE_MIX 那条是同一句话的两个版本；占比的实际读数现在还在分部占比那张图上，
    # 于是同一件事页上有三份措辞。留一个指路，数与判词各归各处。
    # 指路不引标题原文：抬头一改，引号里的话就成了页上查不到的假引文（本文件在页尾
    # 「数据源」那条上刚栽过一次同样的错）。改成描述它讲什么。
    '本家的单月同比分布被 NRE 里程碑拉得很宽（这层结构写在页尾讲月营收 lumpy 的'
    '那一条里，占比的逐月读数在分部占比那张图上），'
    '而引擎的色阶是<b>线性</b>插值、没有 log 入口 —— '
    '尾巴一长就可能把大多数格子挤进同一个色带。'
    '<b>这件事在构建期量过，实测数与判词写在那张矩阵自己的图注里</b>'
    '（拿本表实际画出来的那几年现算，不是拿全序列算的，所以读者照着核对得上）；'
    '本条只说本页为什么这么选口径。'
    '<b>这张图读的主要是格内数字与同月列的纵向对照，不是靠颜色排名。</b>'
    '另外矩阵第一行是序列首年的<b>次</b>年：单月同比要 12 个月的分母，'
    '首年整年没有可比基数，不是漏了。'
    '这张图用单月同比而不是滚动口径，是因为它的命题就是「同一个月份跨年怎么走」——'
    '换成 12 个月滚动合计，每一格都会混进另外 11 个月，行列结构本身就没了。'
    '若哪天图注里那个拥挤度占比冲高，该做的是换口径（底座的 '
    '<code>window.heat_metric</code>，标题与各处点名条会自己跟着改）或换图型，'
    '不是把矩阵留在页上假装能读。')

_NOTE_DAYS = (
    '<b>本页不做日均化。</b>'
    + ((f'把 <code>(m/m) ~ (当月天数变化%)</code> 在本序列 {_DAYS["n"]} 个月上回归，'
        f'斜率是 <b>{_DAYS["slope"]:.2f}</b> 而不是 1；'
        f'2 月对相邻 1、3 月均值的实际比值 {_DAYS["feb"]:.2f}'
        f'（{_DAYS["feb_n"]} 个年度平均），天数比值却是 {_DAYS["feb_days"]:.2f} —— '
        '2 月并没有按它少掉的天数等比例少收。'
        if _DAYS else
        '按天数归一化要求「多一天 ≈ 多一天的产出」，而本序列的月度波动幅度远大于'
        '天数差本身能解释的部分（本次未能从 CSV 现算出斜率，故只作定性表述）。'))
    + '本家还多一层理由：它是<b>无晶圆厂</b>的 ASIC 设计服务商，营收由出货与 NRE 里程碑'
      '确认驱动，「生产日」这个概念在它身上根本不成立。'
      '天数只是农历年与季末拉货日历的<b>代理变量</b>；按天数除一遍会把农历年效应算成'
      '「经营性走弱」。季节性改用同月对同月定位（热力矩阵与汇总表的 y/y 列）。')


SPEC = {
    'ticker': 'guc',
    'name': 'GUC',
    'tracker': 'GUC Monthly Revenue Tracker',
    'title': '创意电子 GUC (3443.TW)：月度营收跟踪',
    'source': 'Source: GUC investor relations monthly revenue disclosure '
              '(guc-asic.com, "Historical Monthly revenue"), cross-checked against '
              'TWSE MOPS t21sc03 and TWSE OpenAPI t187ap05_L; '
              'format after Goldman Sachs GIR',
    # ⚠️ source_zh 与 format_source 会同时进 subtitle（page.js 的 set() 走 **textContent**）
    #    与页尾说明（innerHTML）。所以这两个串必须是**纯文本**：写 <code> 会被原样印在
    #    副标题上（verify_pages.py 的 TEXT_ONLY 会直接判 ERROR），写 &amp; 会印出五个字符。
    'source_zh': 'GUC 官网 IR「Historical Monthly revenue」官方 xlsx'
                 '（单 sheet revenue breakdown，单位 NT$K，含 Turnkey / NRE & Others '
                 '逐月拆分，未经会计师查核），与 MOPS 月营收彙總表、TWSE OpenAPI 交叉核对；'
                 '台湾法定次月 10 日前公布，本家惯例次月 5 日',
    'csv': _CSV,

    # 主序列。**显示单位是 NT$mn 不是 NT$bn** —— 这一条跑过反事实，不是审美偏好，
    # 理由写在 _NOTE_PRECISION 里（柱值标签写死 f0，bn 口径下只剩个位数）。
    'value': {'col': _COL, 'div': 1.0, 'label': 'NT$mn', 'sym': 'NT$',
              'dec': 0, 'unit': 'mn',
              # 官方原始单位是 NT$K；本列 = 官方千元 ÷ 1000，3 位小数逐位无损。
              # 核对表印 3 位小数，读者看到的每一位都是官方数字（见 _NOTE_PRECISION）。
              'raw_label': 'NT$mn', 'raw_dec': 3,
              'zh': 'NT$ revenue', 'ccy_zh': '新台币',
              # 见文件头 B：七期查核／核阅数逐位对过，构建期还会重跑一遍。
              # 这是季度桥 / YTD / TTM 滚动同比合法的前提，不是打个标记而已。
              'summable': True},

    # `official_yoy` 不给：CSV 没有这一列。**但公司的法定申报是有同比的**，
    # 底座只陈述「本页未登记」这个状态，事实断言由 _NOTE_OFFICIAL_YOY 带出处说（文件头 F）。

    # `alt` 不给：本家只有一个计价口径（新台币原生记账），没有第二计价列要核对。

    # ── 汇率：序列照给（`fx_rate` 画得出来），美元腿那两张显式跳掉（文件头 D）──────
    # 这两件事**不是同一件事**，捆在一起跳是本轮修掉的那个错：
    #   · `fx_rate` 画的是汇率**本身**，一条宏观序列，挂同一份汇率的每一页逐点相同，
    #     不需要本家披露任何东西 ⇒ 该出；
    #   · `fx_lines` / `fx_contrib` 画的是**本家的美元营收**，本家没有官方美元数 ⇒ 该跳，
    #     理由逐条写在 skip_note 里（底座硬性要求：skip 的每个 slug 都要有一句理由）。
    'fx': {
        # 全站共用的那一条 NTD/USD（tsm / alchip 等页用的是同一份文件、同一列），
        # 所以本页的汇率线与那几页逐点相同 —— 这正是它不需要本家披露任何东西的原因。
        'csv': 'tsm_fx.csv', 'col': 'ntd_per_usd', 'quote': 'NTD per USD',
        # 纯出处短句，不带「Exhibit source:」前缀（前缀由底座在图脚那一处加）。
        'src': '美联储 H.10 台湾地区日度牌价的月度算术平均（该月全部营业日；'
               '与 FRED 的 EXTAUS 同定义，落库实现见 fetch/tsm.py），'
               'series/tsm_fx.csv 自 2013-01 起，全站共用一份',
        # ⚠️ per-ticker，不可继承。本家**没有任何计价货币占比**的官方表述可引，
        #    所以这里走底座允许的退路：<b>不带数字的定性版本 + 可点开的出处</b>。
        #    不要从 /tsm/ 的「约七成」继承一个数过来 —— 那在本家是一句无源的编造。
        'usd_share_note': {
            'en': 'GUC states that its operating activities are mainly denominated in '
                  'foreign currency, but it discloses neither a US-dollar revenue '
                  'amount nor a US-dollar share. This exhibit therefore plots the '
                  'exchange rate itself, not any GUC revenue line.',
            'zh': '创意电子的营运活动主要以外币计价（合并财报「金融工具 · 市场风险 · '
                  '外币风险」一节的原话），一条外币汇率因此在推着它的新台币报表增速走；'
                  '但公司从不披露美元营收金额、也不披露以美元计价的占比，'
                  '所以「推了多少」在本家无从对账 —— 页上只画汇率本身，不折美元营收腿',
            'src': '创意电子 2026 年第二季合并财务报告（会计师核阅）附注 28 '
                   'FINANCIAL INSTRUMENTS 之 Market risk → Foreign currency risk；'
                   '同一份的附注 18b Disaggregation of revenue 把收入按 Production／'
                   'Region／Application Type 三维切，没有一维是计价货币。'
                   '出处给落地页 https://www.guc-asic.com/en/investor/financial '
                   '（Financial Reports → 2026 → Q2 Consolidated Financial Report；'
                   '直链含上传日、每期一换，写死必然过期）。'
                   '⚠️ 本家没有任何计价货币占比的官方表述可引，所以上面那句是'
                   '不带百分比的定性版本，不是漏写了一个数',
        },
    },

    # 「有腿也不画」的那两张 —— 这正是 skip 的用法（文件头 D）。
    'skip': ['fx_lines', 'fx_contrib'],
    'skip_note': {
        'fx_lines': _SKIP_FX_LINES,
        'fx_contrib': _SKIP_FX_CONTRIB,
    },

    # 逐图补注。键是 slug（写错在 validate 里硬失败，不会静默丢一段口径说明）。
    # 两段都只讲**本家的分部口径**，图型逻辑一行没有。见上面 _NX_* 的说明。
    'note_extra': {
        'rev_bar': _NX_REV_BAR,
        'mix': _NX_MIX,
    },

    # 官方逐月给的业务拆分 —— TSMC 月报里没有这一层，是本页真正的差异化信息。
    # ⚠️ 它进**六**处（本轮从四处变六处，底座加了两张图）：
    #      ① 月营收柱的堆叠段  ② 分部占比时序（本页独有的那张）  ③ 全历史图的两条分部线
    #      ④ 汇总表的两行      ⑤ 核对表的两列                  ⑥ brief 第 4 句的
    #                                                            「合并 ≡ 各分部之和」构成句
    #    本文件仍然不为此写任何图型代码 —— 加的是 note_extra 的两段口径散文，不是图。
    #   label → 英文语境（图例、表头、堆叠段名、占比线名）；zh → brief 与页尾的中文句子。
    # ⚠️ 第二段的 zh 本轮从「委托设计（NRE & Others）」改成现在这个：旧写法在 brief 里
    #    印出「委托设计（NRE & Others）占 14%」，等于用中文把这一格**定义**成委托设计，
    #    而它是 NRE 与 Others 两类收入并成的一格（见 note_extra 的两段）。
    #    页面别处刚说「这不是一项业务」，brief 里就不能再管它叫委托设计。
    'segments': [
        {'col': _TURNKEY, 'label': 'Turnkey',
         'zh': '量产（Turnkey）'},
        {'col': _NRE_COL, 'label': 'NRE & Others',
         'zh': '委托设计与其他（NRE & Others）'},
    ],

    'window': {
        # **显式 None = 用序列自己的起点**（2017-01）。见文件头 G：这是一个决定，
        # 不是忘了写。不写 '2016-01'（本序列没有 2016 年，且真补上更早的合并营收会得到
        # 一段没有分部拆分的历史），也不砍短（会与副标题的「共 N 个月」自相矛盾）。
        # 半导体组里序列晚于 2016 的 ASE（2018-05）与 MTK（2018-01）同样是 None。
        'x_from': None,
        # 单月同比要 12 个月 lag ⇒ 序列自 2017-01 起、同比自 2018-01 起，
        # 到最新年份正好 9 个年度，9 行把「有同比的年份」全部收进矩阵，不多留空行。
        # 这是**实数**不是上限：2027 年起会自然开始滚掉最老的一年（与 /tsm/ 同行为）。
        'heat_years': 9,
        # ⚠️ 走 `_CHECK_ROWS` 常量：`note_extra['mix']` 里那句「印出来的 N 个月里…」
        #    数的就是这张表的行，两处写死会分叉成一句对不上的话。
        'check_rows': _CHECK_ROWS,
    },

    # `breaks` 为空：2026-01 官方把 NRE 与 Others 并成一行是**披露格式**变化，
    # 本页那一列自 2017-01 起始终等于审计口径的 NRE&IP（对出来的，见 _NOTE_SEG_BREAK）。
    #
    # `continuity` 给，但把**适用范围**写进句子本身 —— 出处覆盖的是最近年度，
    # 更早年份要自己回溯各年年报。底座只负责「有出处才印」，管不了出处覆盖多宽，
    # 所以那层限定必须由这句话自己带上。
    'continuity': {
        'zh': '创意电子 2025 年度年报「因併購而發行新股：無」，关系企业一节列的全是'
              '全资自设子公司（GUC-NA／Japan／Korea／Europe／CN），全文无重述说明；'
              '本页序列另与 MOPS 法定月营收申报在 2017-01 至 2025-12 间抽查逐位相等，'
              '两个独立官方源对得上，且逐年加总与年报的查核营业收入逐位相等'
              '（构建期重跑，见页尾「季度聚合…是合法的」一条）。'
              '⚠️ 该出处覆盖的是最近年度，更早年份的合并范围请自行回溯各年年报 —— '
              '并购／处分／重述那一侧本页没有复算，只指路',
        'url': _IR,
    },

    # ⚠️ 与 source_zh 同：这一串进 subtitle（textContent）与页尾（innerHTML），
    #    必须是**纯文本** —— 不写 <code>、不写 &amp;。
    # ⚠️ 旧版只说「少两张图、编号顺次前移」。本轮起本页还**多**一张 /tsm/ 没有的
    #    分部占比图，而月营收柱也改成了分部堆叠 —— 只说「少两张」是把差异说漏了一半，
    #    「编号顺次前移」更是从净减两张变成了净减一张，照抄会把读者往错的编号上带。
    'format_source':
        '版式仿 Goldman Sachs GIR 台股月营收报告（「Hon Hai (2317.TW)」与 '
        '「Wistron (3231.TW)」的 Exhibit 1-2，外加 GS HKEX 深度的超长历史层）；'
        '与 /tsm/ 同一套图列，但两头都不一样：本页因没有官方美元数而少'
        '「本币 vs 美元」「汇率贡献」两张，又因官方逐月给业务拆分而多一张'
        '「分部占比」、月营收柱也按分部堆叠；月均汇率那张照出（画的是汇率本身）',

    'notes': [
        _NOTE_SOURCE,
        _NOTE_ADDITIVE,
        _NOTE_PRECISION,
        _NOTE_MIX,
        _NOTE_SEG_BREAK,
        # ⚠️ 旧版这里还有一条 `_NOTE_NO_USD`（「本页没有美元腿，是刻意的」）。
        #    页上补了汇率线之后它有两个去处更合适：逐图的理由进 skip_note（页尾自动成条），
        #    「有汇率线但没有美元腿」这件事由底座按 EX 现算的那一条统一讲。
        #    留在这里会变成第三份说法 —— 三份说法迟早对不上。
        _NOTE_NO_GUIDANCE,
        _NOTE_OFFICIAL_YOY,
        _NOTE_START,
        _NOTE_HEAT,
        _NOTE_DAYS,
    ],

    'footer': '图表与派生算法源自本机 <code>monthly-op-dashboards</code> 项目 · '
              '仅供个人研究，不构成投资建议',
}
