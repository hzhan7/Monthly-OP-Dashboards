# 数据契约：build/&lt;t&gt;.py → data/&lt;t&gt;.js → assets/page.js + assets/charts.js

每家公司的生成器只做一件事：把 `series/*.csv` 算成一个 JSON payload，写进 `data/<t>.js`。
页面不做任何计算 —— 同一个数字在 Python 和 JS 里各算一遍，迟早会出现图上与表里对不上
而没人发现。**所有数值、所有格式化、所有口径判断都在 Python 侧完成。**

写法：

```python
with open(f'data/{t}.js', 'w', encoding='utf-8') as f:
    # 构建日期只写首行注释，不进 payload —— 进了 payload，monthly_run 的
    # 「data 有没有实质变化」检查（忽略首行的正文比较）就永久失效，
    # 每天都会产出一个内容相同、只有日期变了的 no-op commit。
    f.write(f'// 由 build/{t}.py 生成于 {datetime.date.today().isoformat()}，请勿手改\n')
    f.write('window.DASH = ')
    json.dump(payload, f, ensure_ascii=False, separators=(',', ':'))
    f.write(';\n')
```

---

## 1. payload 顶层

| 字段 | 必需 | 说明 |
|---|---|---|
| `ticker` | ✓ | 小写，与目录名一致（`cme` → `/cme/`） |
| `tracker` | ✓ | 抬头左侧与浏览器标题，如 `CME Monthly Volume Tracker` |
| `title` | ✓ | H1，如 `CME Group (CME): 月度成交量跟踪 — 2026年7月` |
| `data_through` | ✓ | `'YYYY-MM'`。**页面的新鲜度信号绑它，不绑构建日期** |
| `through_label` | ✓ | 人话月份，如 `2026 年 7 月`；零售日历公司可写 `零售月 Jun 2026（5 周）` |
| `subtitle` | ✓ | 副标题：数据源 + 覆盖区间 + 版式出处 |
| `headline` | ✓ | 一行数据条。正负号交给 f-string 的 `+` 标志，别写死字面量（负值会印成 `+-0.6%`） |
| `brief` | | headline 之下、Exhibit 1 之上的一段解读（HTML，走 `innerHTML`）。职责与 `headline` 互补：那一行给**读数**，这一段给**读数该怎么读**（基数效应、口径背离、所处区间）。**随月份变，每月重写。**整段在 Python 侧拼好 —— 页面不做措辞判断。不给就整块 `hidden`，不会留下一个空的带框方块 |
| `glossary` | | `brief` 之下、Exhibit 1 之上的名词释义（HTML，走 `innerHTML`）。与 `brief` 分工：brief 说「**这个月**这组读数该怎么读」，glossary 说「**这些词**是什么意思」（ADV、RPC、口径断点……）—— 所以它**不随月份变**，一年到头是同一段。合进 brief 的后果是每月重写 brief 时要连释义一起重抄（迟早抄漏或抄旧），而且当月真正的判断被淹在常年不变的定义里。版式是定义列表：`<h4>名词释义</h4><dl><dt>词</dt><dd>释义</dd>…</dl>`（`.glossary` 的 CSS 只认这个结构：`dl` 是两列 grid，`dt` 在左、`dd` 在右）。同样不给就 `hidden` |
| `hub_line` | | 首页卡片上的短摘要（≤60 字）。不给则截取 `headline` 前 110 字 |
| `source` | ✓ | 所有 exhibit 共用的 `Source:` 行 |
| `source_date` | | 官方发布日 `YYYY-MM-DD`，显示在抬头右侧。**必须是源头自己写出来的日期**（文件里的 "Published on"、新闻稿电头、SEC filingDate，或该家 fetch docstring 已认定为发布日的 HTTP `Last-Modified`）。构建日、文件 mtime、下载时刻一律不行 —— 那是一句关于外部世界的事实断言，编不得。台账在 `series/source_dates.csv`，由 `fetch/<t>.py` 摄入该月时按月份钉进去、`build/<t>.py` 查表；**不要在 build 里当场从 `cache/` 解析**（cache 是 gitignore 的，清掉后这半句会静默消失，且缓存里可能躺着比 `data_through` 更新的一期文件，当场解析会把新一期的发布日安到旧月份上）。横截面页用 `latest_of()` 取成员里最晚的那个，缺任何一个成员就整体省略 |
| `source_date_note` | | 源头**根本不标**发布日时，改印这句话（如 MSCI 的「官方未标注发布日」）。只在 `source_date` 缺席时渲染。用于「这是那个源的固有属性」，**不要**用来搪塞「台账这个月还没记上」那种临时缺口 —— 后者应当留白并等它自愈 |
| `stale_source` | | `True` 时抬头打红标「官网下载失败，本次沿用本地缓存」。**目前没有任何生成器会产生它** —— 现有的 fetch 模块下载失败一律抛异常，那家整个跳过、`data_through` 原地不动，由首页红点报警。渲染分支保留着，但在有 fetch 真走「降级到旧缓存」这条路之前，不要在生成器里设它 |
| `xlabels` | ✓ | 短窗口 x 轴标签数组 |
| `xlabels_long` | | 长历史 x 轴标签数组；exhibit 写 `x: 'long'` 时用它 |
| `summary` | ✓ | Exhibit 1 汇总表，见 §2 |
| `exhibits` | ✓ | Exhibit 2..N，见 §3 |
| `table` | | 末尾核对表，见 §4 |
| `notes` | ✓ | 「口径与方法说明」的 `<li>` 数组，允许 HTML |
| `footer` | | 页脚 HTML |

## 2. summary（Exhibit 1 汇总表）

```python
'summary': {
  'title': '2026 年 7 月汇总 —— 本月 vs 上月／去年同月，及近 3 年分位',
  'heads': ['本月', '上月', '去年同月', 'm/m', 'y/y', '3Y %ile'],
  'sep':   3,          # 第几列前画竖分隔线（水平值与变化率之间），0-based
  'rows': [
    {'kind': 'group', 'label': '成交量'},                    # 板块分隔条
    {'label': 'ADV 千张', 'cells': [
        {'v': '30,100'}, {'v': '28,400'}, {'v': '26,900'},
        {'v': '+6.0%', 'cls': 'pos'}, {'v': '+11.9%', 'cls': 'pos'},
        {'v': '88', 'cls': 'hi'}]},
  ],
  'note': '分位为近 36 个月百分位。…',
}
```

`cells[i].v` 是**已经格式化好的字符串**，`cls` 取 `pos` / `neg`（绿/红）、`hi` / `lo`
（分位高/低）或空。格式化规则属于口径，必须在 Python 侧决定：

- **比率类指标的差异一律用 pp / bp**，不用百分比变化（GS LPLA 规矩 2）。
  `abs(v) < 1` 时用 bp，否则用 pp。
- **反向指标**（逾期率、坏账率等，越低越好）算好之后再决定 `cls`，不要让页面猜。
- **分位对单调序列没有意义**：仓库数、累计资产这类几乎只增不减的序列，分位永远是 100，
  是噪音不是信息。判据：`diff >= 0` 的比例 ≥ 90% 就把该行分位留空（`{'v': ''}`）。
- **4-4-5 零售日历**下净销售额的 m/m 不可当趋势读（5 周月 vs 4 周月），要么留空要么在
  `note` 里写明。

## 3. exhibits

公共字段：

| 字段 | 说明 |
|---|---|
| `n` | Exhibit 编号（汇总表占 1，图从 2 起） |
| `kind` | 见下表 |
| `title` | 图标题。GS 的做法是标题即结论，含当期数字 |
| `fmt` | 数值格式键：`f0` `f1` `f2` `f0c`(千分位) `int` `pct0` `pct1` `pct0z` `pp0` `pp1` `x0` `usd0` `usd1` `usd2` |
| `yfmt` | y 轴刻度格式键；不给则按步长自适应 |
| `label_fmt` | 柱顶/点上数值标签的格式键；不给则退回 `fmt` |
| `ylab` / `ylab2` | 左轴 / 右轴标题 |
| `note` | 图下方 `Note:` 行 |
| `src_extra` | 追加在 `Source:` 行后的补充说明 |
| `x` | `'long'` 时用 `xlabels_long`，否则用 `xlabels` |
| `xlabels` | 该图自己的 x 轴标签（覆盖上面两者） |
| `xstep` / `xrot` | x 轴标签的抽稀步长 / 旋转角度 |
| `full` | `True` 走通栏（127 根柱塞进半栏每根不到 3px，必须通栏） |
| `height` | 覆盖默认高度 |
| `legend` | 单序列图的图例文字 |
| `annot` | 图内注解 |
| `zero_line` | 强制画零线 |

**基线规范字段**（这几条是硬约定，见 charts.js 头部注释）：

| 字段 | 语义 |
|---|---|
| `ycap` / `yfloor` | 截轴上/下界。**截轴不删点**：超界的柱画到 cap 并加断口符号，超界的点钳位画红色空心圈，真值一律竖排标出 |
| `cap_note` | 截轴时图顶的斜体小字，如 `axis capped — outliers shown in red` |
| `break_at` | 结构性断点的 x 索引（int 或 int[]），红色虚线画在该期柱的**左缘**，语义是「从这一期起与左侧不可比」 |
| `break_label` | 断点竖排标签；给一个字符串时所有断点共用 |
| `bar_marks` | 需要画成斜纹的柱的索引（口径调整月、53 周月等） |
| `mark_note` | 斜纹柱在 tooltip 里追加的说明 |

### 已有 kind（COST / IBKR 在用，不要改动其行为）

| kind | 数据字段 | 用途 |
|---|---|---|
| `gs_bar` | `values[]`, `legend`, `avg12`, `yoy_txt`, `mom_txt`, `yoy?`, `stacks?` | 柱 + 12月均线 + 每柱数值 + YoY/MoM 气泡。对应 `gsx.lvl_bar`。**给了可选的 `yoy{values,color,yfmt,name}` 就改画次轴 y/y 折线、不画均线**（deck 的原样）。**给了可选的 `stacks[{name,color,values}]` 就把柱按业务分色堆叠，总额仍取 `values`**（轴、柱顶数值、均线、次轴 y/y、表格视图全部照总额走；各段之和 ≡ `values` 是硬校验，配色不得与 `yoy.color` 撞）。两者见 `engine_kinds.md` §8 |
| `gs_line` | `values[]`, `ovals_at_bottom?` | 平滑线 + 每点数值。对应 `gsx.chg_line` |
| `gs_line_avg` | `values[]`, `avg12`, `avg_label` | 平滑线 + 均线 + 右端均值标注 |
| `lines` | `series[{name,color,values,dash?}]`, `markers?`, `zero_base?`, `end_label?` | N 条线。对应 `gsx.long_line` / `gsx.indexed_lines`。**长历史图（对应 `long_line` 的）应当给 `zero_base: true`（deck 是零基线，不给等于隐性截轴）与 `end_label: true`（deck 会标最新一点）**，见 `engine_kinds.md` §8 |
| `lines_endlabels` | 同上 | 多条平滑线，仅两端标数值。对应 `gsx.multi_line` |
| `bar_line` | `bar{color,values,yfmt}`, `line{color,values,yfmt}` | 柱 + 线（同一轴） |
| `bar_line_dual` | 同上 + `ylab2` | 柱（左轴）+ 线（右轴）。对应 `gsx.rev_bar_yoy` |
| `stacked_dual` | `stacks[{name,color,values}]`, `line{...}`**可选** | 堆叠柱 + 右轴线。对应 `gsx.stack_share`。**两种用法**：①**占比 / 份额堆叠**（各段之和恒为 100，段值全非负，轴自 0 起）—— 这是它最早也是现网 22 张图**唯一**的用法；②**可正可负的贡献分解**（2026-09 起，如 `/cost/` 的可比销售 = 客单价 + 客流，客单价某季为 −0.9%）：正段自 0 往上堆、负段自 0 往下堆，左轴下界跟着最深的负向累计走，零线自动画出，**柱的总高不再等于列合计**。**不给 `line` 就退化成纯堆叠柱**（右轴、图例项、表格行、以及柱顶 28% 的留白一并消失）—— 占比型堆叠里段高本身已经把每一块读出来了，再拿其中一段换个刻度画一遍是同一个数说两遍。**例外**：某一段常年只占几个百分点时，它那几 pp 的变化在 0–100 的堆叠里量不出来，这时才给 `line`（判据是「最矮那段量不量得出来」，不是段数）。⚠️ **用法②不要给 `line`**：右轴仍写死 `r0 = 0`，与跨零的左轴对不齐，必然触发「两轴独立缩放」的红字兜底。⚠️ 用法②也不要开段内 `label`：那个标签硬编码 `comma(v, 0)`，−0.9 会印成 `-1`。详见 `docs/CHART_KINDS.md` §3.14 与 `build/engine_kinds.md` §10 |
| `diverging_bars` | `values[]` | 正负分色柱 |
| `bars_labeled` | `values[]`, `annot?` | 柱 + 每柱数值 + 注解 |

颜色用色名字符串（`NAVY` `BLUE` `MBLUE` `GRAY` `GREEN` `GOLD` `RED`），不要写十六进制 ——
这套配色过了色盲安全校验，另起新色会破坏它。

### 新增 kind

见 `engine_kinds.md`（由图表引擎那一侧维护）：
`heat_matrix` `year_lines` `qtr_bar` `seasonality` `bridge_bar` `grouped_bars` `range_band`。

### `kind: 'table'` —— 夹在图中间的 HTML 表（**不是引擎的 kind**）

用途是让一张表**参与图的阅读顺序**：单位经济性、口径对照这类表，读者是在看完某几张图
之后立刻要看的，推到末尾附录里等于让他翻回去找。§4 那张末尾核对表职责不同 ——
它是拿去和公司披露逐条核对的原始值，位置就该在最后，两者不要混为一谈。

字段与 §4 的 `table` **完全相同**（`n` / `title` / `idx` / `cols` / `rows`），另加两个可选项：

| 字段 | 必需 | 说明 |
|---|---|---|
| `kind` | ✓ | 固定 `'table'` |
| `n` | ✓ | Exhibit 编号，与图排在同一条序列里（1 号被汇总表占着，图从 2 起连号） |
| `title` | ✓ | 卡片抬头渲成 `Exhibit n: title`（前缀由 page.js 加，别自己写进 title） |
| `idx` | | 首列表头，缺省「月份」。这种表的首列通常**不是**月份，记得给（如「项目」） |
| `cols` | ✓ | `[[列名, 列键], …]`，不得为空 |
| `rows` | ✓ | `[{'xl': 首列文字, 列键: 值, …}, …]`，不得为空 |
| `full` | | `True` 走通栏（加 `.wide`，`grid-column: 1 / -1`）。半栏宽只有 ~570px，超过 4–5 列就该开 |
| `note` | | 卡片下方的 `Note:` 行，位置与用词跟图卡一致（`<p class="src"><b>Note:</b> …`），允许 HTML |

值一律是**已经格式化好的字符串**（或 `None` → 渲染成「—」），与 §4 同规矩：
页面不做计算，也不做格式化。

**末行不高亮。** 全局表格样式里那条「最后一行加粗 + 蓝底」是给 §4 核对表的最新月行准备的
（那张表的最后一行按构造就是最新一期）；这种表的最后一行只是又一个普通项目，
所以 page.js 给它的卡片挂了 `.tblex`，CSS 把那条关掉 —— 表里没有合计行时，
加粗末行等于凭空暗示「这是合计」。要一行合计就自己写进 `rows`，别指望版式替你标。

**charts.js 不认得这个 kind，也不该让它认得。** 拦截在 `assets/page.js` 的 exhibits
循环里：`ex.kind === 'table'` 那一支在 `window.Exhibits.card()` **之前**就 return 了。
漏掉这道拦截的后果不是「这张表画不出来」，而是**整页从这里往下全空** —— 引擎的 kind
分派没有这一支，未知 kind 掉进 `else` 当 `values[]` 柱图，而这种 exhibit 根本没有
`values`，于是抛 TypeError，本页此图之后的 exhibit 一张都不渲染（跟「缺必填字段」
同一种后果）。理由见 `docs/CHART_KINDS.md` 开头那句「引擎不许改」：它同时服务全站
34 张页，为一个不画图的 kind 动它要重新验收 34 页。

`build/verify_pages.py` 因此把它放进 `PAGE_KINDS` 而不是 `KINDS` —— 后者的语义被钉死成
「assets/charts.js 认得的全部 kind」，混进去只会让下一个对着它点引擎源码的人得出
「引擎少实现了一种」，然后去改那份不许改的文件。

## 4. table（末尾核对表）

```python
'table': {
  'n': 20, 'title': '近 13 个月月度指标核对表（官方原始单位，未换算）',
  'idx': '月份',                       # 首列表头，默认「月份」
  'cols': [['ADV 千张', 'adv'], ['交易日', 'days']],
  'rows': [{'xl': 'Jul-26', 'adv': '30,100', 'days': '22'}, ...],
}
```

`rows` 里的值同样是**已格式化的字符串**（或 `None` → 渲染成 `—`）。这张表的作用是让人
拿它和公司披露逐条核对，所以**保持官方原始单位，不做任何换算**。

## 5. 每个生成器都要遵守的几条

1. **推导值必须标 Implied。** 标题带 *Implied* 的图不是公司披露值，假设写进 `note`。
2. **口径断点必须画出来**，不能靠图注文字提一句就算数（`break_at` + `break_label`）。
3. **不可比的相邻期不能画成连续序列**：缺口不要用直线连，宁可断开。
4. **窗口选择**：近期图固定 13 个月（够算 y/y 首末对比与 prior-12mo 均值）；
   长历史图用全序列并标 `full: True`。
5. **失败要响**：解析结果缺列、月份对不上，抛异常让 `monthly_run.py` 记 FAIL，
   **绝不静默写 NaN 上线**。

   这条不靠自觉：写文件一律走 `build/payload_guard.py` 的 `write_dash(path, payload, ticker)`，
   它在写出**之前**递归扫 payload，发现 NaN / Infinity（含已被 f-string 格式化成
   `$nanbn` `nan%` 的展示串）就报出具体路径（`exhibits[7].values[3]` + 该图编号标题）
   并 exit 1，旧的 `data/*.js` 原地不动。新增生成器**不要**自己写 `json.dump` ——
   `json.dump` 默认 `allow_nan=True`，写出的裸 `NaN` 是非法 JSON 但合法 JS，
   浏览器照渲染，退出码还是 0。
   `null` 不在拦截范围：缺月、未满季作废的 y/y、Cboe 滞后一月的 RPC、HKEX 月中才披露的
   new_listings 都是合法留空，一律走 `L()/LN()` 出 `null`。
## 6. 同比口径（2026-09 按页面所有者的指令定；实现在 `build/yoy.py`）

**全站同比只有一种口径：单月同比**（当月 ÷ 去年同月 − 1，流量与存量各按下面的
第 1、2 条取）。除了 §6.2 那张点名的例外表（两页五张，都不是折线），
页面上**一条 12 个月滚动合计的同比都不画**。

这条规矩是页面所有者定的，原话：

> 这里所有公司，里面有 12 个月滚动同比的折线图，都帮我换成简单的 yoy 同比折线图。
> 我就需要直接的月度数据 yoy 同比折线图，不要给我搞 12 月滚动合计同比。

**它推翻了 2026-08 全站审计定下的上一版 §6**（那一版把 12 个月滚动合计的同比定为
流量的默认口径）。推翻的不是那次审计的**测量**——下面 §6.5 把它整段留着，一个数都
没删（2026-09 逐条复算过，对得上的与对不上的都标在原数旁边）——而是它的**结论**。
两者的关系要说准，否则下一个人会以为审计错了：

- 审计测的那件事是真的：**单月同比比滚动口径毛刺大得多，而且单看一个月能把方向读反。**
  它当时那批序列里，2026-09 复算得出来的是「65% 的序列至少有一个月两种口径符号相反」
  （复算 138/212，审计记 147/225。⚠️ **这里的绝对条数是 2026-09-02 20:4x 那份
  `data/` 快照的读数，分母每 build 一次就动** —— 同日 21:4x 重跑是 138/213，
  占比仍是 65%；三次读数与配方见 §6.5 那张表，别拿这两个数去对今天的仓）——
  但那是在**当时的图窗（近 25 个月）**上量的，
  换到今天的图窗（2016-01 起）同一批序列是 93%。审计那三个数里另外两个的下场不同：
  「放大 2.08 倍」在审计自己的分母上**复算不出来**（复算 1.87 倍），
  「跳变中位 30pp vs 4.8pp」是**统计量名标错了**（按字面复算只有 7.9 / 1.4pp，
  读成「逐序列最大跳变的中位」才是 26.8 / 4.6pp）—— 逐条见 §6.5。
  **引用这批数一律要带窗口**，不带就是下一次翻车。
- 审计当时要解决的问题也是真的：`cme` Ex2 说 Jun-26 是 +19.1%、同页 Ex8 说 2026Q2
  （⚠️ 2026-09 按页面所有者指令，`cme` 那张季度合计柱（当时的 Ex8）已删除，同页的
  bridge_bar 也改成了月度刻度 —— 下面这两个读数是**当时**的证据，规矩本身不受影响；
  §6.3 给 `bridge_bar` 的豁免理由「横轴不是月」对 `cme` 那张已不再成立，属假阴性）
  是 −1.2%，同一批合约、同一张页、符号相反，读者没有线索知道该信哪张。
- **但那个问题的病根是「同一页混用两种口径」，不是「用了单月口径」。** 统一成滚动
  能解决它，统一成单月同样能解决它——而后者是页面所有者要的那一种：他要读的就是
  「这个月比去年这个月」，一个不含 12 个月平滑的、拿柱子直接除得出来的数。

代价没有消失，只是改由**每张图自己**承担并写明（见第 3 条）。

### 6.1 五条硬约定

1. **流量序列用单月同比。** 成交量、成交额、募资额、净新增资产、月营收 ——
   走 `yoy.mom_yoy(s, yoy.FLOW)`。**柱与线取自同一列**：拿这根柱除以 12 根柱之前
   那根，就是线上这一点。这是它相对滚动口径唯一的、也是决定性的好处 ——
   读者能自己核对。

   ⚠️ **例外存在，而且必须逐张声明。** 上面那句「拿柱除柱就是线上这一点」是本条的
   卖点，但它**不是无例外的硬断言**：线取自**公司披露值**或**还原值**、而不是取自
   图上那根柱本身时，柱除柱就对不上。这种图**合法**，条件是**在该图自己的
   `src_extra` 或 `note` 里逐张声明**「线不是柱除柱、它的基期/分子是什么」——
   页尾说明不算数（同第 3 条那句「逐图是字面意思」）。没有这句声明的对不上就是 bug。

   **全站只有两张，2026-09-02 逐图扫过三遍**（20:4x / 21:1x / 21:4x，中间隔着
   大半个 `data/` 的重跑，三次结论相同；末一遍是另一个人另写一份脚本跑的）。
   扫法：每一条同比序列 × 同图每一条等长的水平值序列，
   `(bar[i]/bar[i−L]−1)×100` / `bar[i]−bar[i−L]` / 差 ×100 三种
   读法 × L∈{12, 4} 全试，容差 0.06pp。
   ⚠️ **条目数别照抄，重跑** —— 前两遍记的是「可核对条目 247 → 248 条、其中
   245 → 246 条逐点全对」，21:4x 那遍数出的是 **249 条可核对、239 条逐点全对、
   2 条对不上**（另有 8 条同图虽有等长水平值序列、但可比点不足 6 个，没进分子分母）。
   差在**「可比点少于几个就不算一条」这个门槛，前两遍没写下来** —— 这是配方漏了
   自由度，不是谁数错了；**结论那一句（只有下面两张对不上）三遍完全一致**。
   **对不上的自始至终就是下面这两张**：

   - **`cost` Exhibit 4「Monthly Net Sales ($bn) & y/y Growth」** —— 115 个可比月里
     **79 个**满足柱除柱、**36 个**不满足，最大偏差 **26.62pp**（Jan-24：柱算
     +31.12%，线印 +4.5%）。偏差 >20pp 的恰好四个月，全是 53 周财年那四个月：
     Jan-24 26.62 / Jan-18 26.40（+34.80% vs +8.4%）/ Jan-25 20.84 / Jan-19 20.50；
     其余 32 个月偏差都在 5pp 以内（**36 个不合的月合起来**数：偏差 >2pp 的 12 个、
     >1pp 的 16 个 —— 这两个数含上面那四个，别读成「32 个里的」。
     2026-09-02 21:4x 独立重跑，本段每一个数逐位复现 ✅）。
     原因写在该图 `src_extra` 里：「柱 = 净销售额绝对值，未按周数调整（零售月为 4 或
     5 周，4-4-5 日历）；线 = 公司报告 y/y，其基期是同样周数的上年错位窗口，
     与相邻柱不是同一区间」，并逐月点了 Jan-18 / Jan-19 / Jan-24 / Jan-25 四条红色
     竖虚线 —— **声明是够的，这是合法例外**。
   - **`ibkr` Exhibit 2**（净新增账户）—— 116 个可比月里 113 个满足，**三个不满足**：
     3/25（柱算 +26.75%、线印 +49.404%）、9/25（+25.60% vs +92.268%）、
     3/26（+44.89% vs +22.919%）。图注已声明「柱与线不同源…本图是唯一的例外」
     并给出还原值，三个数逐位复算得上：87.7/58.7−1 = **49.404%** ✅、
     111.9/58.2−1 = **92.268%** ✅、107.8/87.7−1 = **22.919%** ✅（分母 58.7 / 58.2
     取自图上去年同月那根柱，3/26 那条的分母是**还原后**的 87.7 而不是柱高 74.4）。
     **合法例外**。

   **不构成例外的三类，别拿去当反例**：① 柱本身就是同比的图（`cost` Ex2/5/7/8/9/10
   是「Core comp vs Reported comp」两条增速并排，图上根本没有水平值柱，本条不管）；
   ② 横轴是季不是月的图（`msci` Ex9 费率图 L=4，按季错位 4 格逐格全对）；
   ③ 图上只有同比线、没有水平值柱的图（如 `asx` Ex4/5 那种 `grouped_bars` 同比图、
   `tsm` / `alchip` / `ase` 三家的双币种同比对照）—— 那不是「柱除柱对不上」，
   是这张图压根没有柱可除，本条的可核对性对它们不适用。
   **这一类有多少张是活数，不在这里存**（加一张同比图它就涨）。数法：遍历
   `data/*.js` 每一张有同比序列的图（登记器用 `tools/check_yoy_caliber.py` 的
   `yoy_series()`，别自己发明一套），看同图有没有任何一条**非同比的、等长的**数值
   序列；一条都没有的就是这一类。
   ⚠️ **数之前先扣掉登记器的一处已知误判，否则会多数出几张**：`yoy_series()` 认的是
   **轴的单位**不是口径（见它自己的 docstring），所以画在比率轴上的 `gs_bar`
   会连**主轴那根水平值柱**一起被登记成同比 —— 那张图其实有柱可除，不属于这一类。
   数的时候把 `gs_bar` 的图级 `values` 一律当水平值。2026-09-02 21:4x 实测，
   不扣这一处会多出 4 张（`axp` Ex7、`lseg` Ex10 / Ex11、`msci` Ex9），
   扣掉之后 `gs_bar` 这一档整个清零。
   ⚠️ **上一版这里写的「全站 36 条」是本轮自己写进散文、又复算不出来的数，留痕在此**：
   2026-09-02 21:4x 照上面这个数法（含上面那处扣减）在当日 `data/` 快照上重跑是
   **70 张**，排除 §6.3 图型豁免后 **39 张**（其中 6 张是 `cost` Ex2/5/7/8/9/10，
   已经在上面 ① 里点过名，两类重叠 —— 去掉它们，只属于本类的是 33 张）。
   哪一种读法都对不上 36；原数怎么来的没查出来。
   同一句里「`mtk` 的双币种同比对照」也是假的：`series/mtk.csv` 只有
   `revenue_ntd_mn` 一列，mtk 页上根本没有这张图 —— 全站有双币种同比对照的是
   `tsm` / `alchip` / `ase` 三家（2026-09-02 逐页核过）。

   **「单月」这两个字必须写进标题，而且必须是在修饰「同比」。** 判据
   （`tools/check_yoy_caliber.py` 的 **R4**）只读 `title` / `ylab2` / `legend` /
   `yoy.name` 这四处，写在图注里不算数。
   ⚠️ **光有「单月」两个字不够了（2026-09-02 判据侧收紧，本条跟着改）**：
   `_MOM_DECL` 现在认的是「单月同比」「单月口径」「单月 y/y」`single-month`，
   以及「单月」与同比类词（`同比` / `y/y` / `yoy` / `变化率` / `growth`）**相邻**的写法 ——
   相邻的判据是 `_MOM_GAP = [^一-鿿]{0,10}`，两者之间**不许夹汉字**。
   所以「证券市场**单月成交额**」这种「单月」修饰的是**水平值**而不是同比的写法
   **不再算数**（照报 🟡），而「证券市场成交额（次轴：**单月同比**）」算数 ——
   判据的 `--selftest` 第 ⑤ 组就是拿这一对当正反例的。
   把话说全了的描述式写法也认，但认的是 `_MOM_DECL` 里那几个固定说法
   （「当月对去年同月」「同月对去年同月」「与去年同月相比」「水平值同比」
   「点对点同比」）—— 自己发明一句大白话（例如只写「当月 ÷ 去年同月 − 1」）
   判据不认，照报 🟡。
   ⚠️ **这一条在 2026-08 那一版里是「为偏离默认口径辩护」**（那时默认是滚动，
   上一版 §6.1 第 2 条要求写进标题**并在图注说明为什么这里该用单月**）。默认翻成
   单月之后，要辩护的那一半没了，要写进标题的那一半留着，但理由换了：让读者一眼
   知道这条线是拿柱子直接除出来的、可以自己核对。**理由不必写，代价必须写**
   （第 3 条）。存量图与比率图不受这一条约束 —— 判据对它们走 `legal_mom` 分支
   直接跳过 R4（存量身份认标题/纵轴/图例里的「存量」「期末口径」「月末」那类字样，
   或列名被 `yoy.classify()` 判成存量；比率身份认「回源命中百分点差那一档」
   或列名被 `classify()` 判成比率），两类都不需要再念一遍「单月」。注意 `legal_mom` **不豁免 R1** —— 存量图若真画了滚动口径的线，
   一样报 🔴。
   ⚠️ **这一条 2026-09 重写 §6 时被整段漏掉了，这次补回。** 漏掉期间机器一直在执行
   一条契约不再声明的规矩：R4 照报 🟡（报错文案原文就是「CONTRACT.md §6：要用单月
   同比必须在标题里写明」）。补在第 1 条里而不新起一条，是因为 §6.1 的 1–5 已经被
   `docs/*.md` 与生成器引用了十几处，加一条要改十几处；而 R4 本来就只对流量开口，
   它天然属于第 1 条的范围。

2. **存量序列走点对点同比** `yoy.mom_yoy(s, yoy.STOCK)`。OI、AUM、市值、账户数、
   余额、在外名义量：把 12 个月末的快照加起来不是任何东西 —— 既不是「一年的量」
   （存量不累积），也不是「平均水平」（没除以 12）。`yoy.ttm()` 对 `kind=STOCK`
   直接抛 `CaliberError`。噪声大用轴范围解决（`ycap` / `yfloor`），不要换口径。
   **默认口径与上一版相同**（上一版 §6.1 第 4 条也把点对点定为存量的默认），
   本条的公式一格没动。⚠️ 但**别写成「这一条与上一版逐字相同」** —— 上一版给了存量
   两个合法口径，第二个是「要平滑就用 12 个月滚动均值的同比」；本轮把那条线一并撤了。
   `yoy.ttm_mean_yoy(s, yoy.STOCK)` 仍然留在模块里，但**页面上不再用它画线**，
   它现在只在图注里当对照量出现。

3. **每一张画<u>流量</u>同比的图都要印出单月口径的代价，用这条序列自己实测。**
   这是本次改口径付的账，不许省。要报的三样：逐月标准差、相邻月最大跳变（带月份）、
   两种口径符号相反的月份数。生成方式：`yoy.describe(yoy.caliber_diff(s, kind, win))`
   ——它拿**这条序列自己**实测，不引别家的例子；对照的那一侧（12 个月滚动）
   **只在图注里以数字出现，页上一条线都不画**。
   底座已有两处现成实现，新生成器照抄别重写：`build/single.py` 的 `mom_cost_zh()`、
   `build/mrbase.py` 的 Ex2 图注。

   **范围为什么限定在流量**：这一条要付的是「本轮换口径」的账，而只有流量列换了口径。
   存量列本来就走点对点（第 2 条，一格没动），它的对照量是 12 个月滚动**均值**同比 ——
   那回答的是另一个问题（去年一整年的平均水平 vs 前年），不是「换口径的代价」；
   比率列连对照量都不存在（第 4 条）。所以这两类**不强制**印这一段；页面愿意印
   对照数字是好事（`schw` 的 `stock_note()` 就印了），但那是额外的信息，不是本条的债。

   ⚠️ **「逐图」是字面意思，页级不算数。** 一段写在页尾的实测代价，读者在看某一条
   金线时够不到；而这一条的全部用处就是让人读那条线的时候知道它有多毛刺。
   页尾那段可以有（点名口径用），但不能顶替逐图那一段。

   ⚠️ **不许写「看着更灵敏」这类理由。** 口径是所有者定的，图注要说的是**代价**，
   不是替口径辩护。同理，也不许在图注里说「滚动口径更好但我们没用」——
   那是替页面上不存在的东西背书。

   ⚠️ **本条 2026-09-02 白天还全站大面积欠着，当天下午补掉大半，机检至今一条都不查 ——
   条文不放松，欠账与护栏缺口都写在这里。**
   条文比现状严是有意的（契约的用处正在于此），但一条「全站不满足、又没有任何护栏」
   的空条文同样没用，所以把两件事都记死：

   **怎么数（照这段跑就能复现，别自己发明分母）。** 遍历 `data/*.js` 的每一张
   exhibit，用 `tools/check_yoy_caliber.py` 的 `build_index()` + `identify()` 把它的
   同比序列回源到某一 `series/*.csv` 列，**该列被 `yoy.classify()` 判为 `FLOW` 的
   才进分母**；「付了账」= 该图自己的 `note` / `src_extra` 里出现「标准差」，
   或出现降级说明（可比月不足 24，`single.py::MOM_COST_MIN`，只能说「报不出比例」——
   **那也算付**，它同样是拿本序列实测出来的结论）。
   ⚠️ **数「付没付」不要只认一种措辞**：底座今天有三套写法（`mom_cost_zh()` 的完整段、
   `_mom_cost_brief()` 的一行式「单月口径的代价：…」、以及可比月不足时的降级句），
   只认其中一种会把另外两种误记成欠账 —— 本节 2026-09-02 就先这么错了一次，
   同一批图数出 18 张，改用三种措辞的并集之后是 12 张。

   **欠多少（2026-09-02 21:0x 实测，`data/*.js` 当时的快照）。**
   回源确定命中流量列的 **106 张**里，付了 **94 张**、**欠 12 张**：
   `heat_matrix` 9 张（`alchip` Ex9、`cme` Ex18、`exchanges-products` Ex4、`hood` Ex28、
   `ice` Ex6 / Ex8 / Ex13、`miax` Ex6、`mtk` Ex7）+ `gs_bar` 3 张
   （`spgi` Ex2、`asx` Ex30、`enx` Ex34）。
   **这 12 是下界不是上限** —— 另有约 80 张图的同比回源判不出列（派生量：ADV × 交易日、
   多列相加、FX 换算过的），它们是不是流量用这套数法根本判不了，没进分母。

   **这 12 张分两类，别混着记**：

   - **真欠账 10 张** —— 9 张 `heat_matrix` + `spgi` Ex2。
     `heat_matrix` 那 9 张要多说一句：一张矩阵有好几条序列，付账的写法是
     **在图注里报总体、并点名最毛刺的那一条**，不是逐行报一遍 ——
     那是写法问题，不是豁免理由。
   - **`asx` Ex30 与 `enx` Ex34 不是欠账，是 `classify()` 认错了列。**
     两张图的标题都写着「（存量，期末口径）」，画的确实是存量
     （`otc_open_notional_audbn` = OTC 未平仓名义额、`listed_funds` = 月末挂牌基金只数），
     而 `yoy.classify()` 被 `_FLOW_PAT` 命中判成了流量（`_STOCK_PAT` 两个都没命中）。
     存量列本条不管（见上面「范围为什么限定在流量」）。
     **这一处该修的是 `yoy.py` 的 `_STOCK_PAT`，不是这两张图。**

   **本轮变化的来处**：`build/single.py` 的 `mom_cost_zh()` 原先只挂在三条路径上
   （`ex_head_bar` / `ex_mix_total` / `ex_level_yoy`），而全站产图最多的
   `ex_single`（groups 单列桶的 `gs_bar` + 次轴单月同比）**从不调它** ——
   2026-09-02 20:5x 按同一套数法量到的欠账是 **59 张，其中 53 张出自底座这一条路**。
   同日 21:0x 另一名 agent 给 `ex_single` 与 `ex_yoy`（头条那张纯同比图）加上了
   `mom_cost_zh(..., brief=True)` 的一行式写法，欠账当场从 59 掉到 12。
   那一行式**三样一个不少**（逐月标准差、相邻月最大跳变带月份、符号相反的月份数），
   所以它是合规的付账写法，不是缩水版。
   **这个数是活的**：加一张流量同比图它就涨，照上面那段数法当场重跑，
   别沿用本节记的任何一个。

   **有没有可以豁免的？今天一张都没有。** 唯一站得住的结构性豁免理由是「横轴不是月、
   滚动那一侧根本算不出来」（横截面图：序列长度就是成员数，见 §6.6 里 R5 那段），
   但上面这 12 张里**横截面图 0 张** —— 逐张查过横轴标签，全都解得出月份，
   `caliber_diff()` 拿它们自己的列跑得动。所以本条不设豁免档。

   ⚠️ **机检侧目前零覆盖。** 逐条读过判据当时的全部规则体（2026-09-02 21:0x 读到 R5，
   23:5x 连后加的 R7 / R8 一起再读一遍）：**没有一条**查「图注里有没有这段代价」。
   所以这笔账**坏了不会响**：新加一张流量同比图而忘了印代价，判据这一侧不会多出
   任何一条 finding、退出码照样 0，与「全站都付清了」在输出上分不开 ——
   正是判据脚本开头自己反对的那种失明。补法见 §6.6 末段。

4. **比率序列的同比一律走百分点差**（`yoy.mom_yoy(s, yoy.RATIO)`），
   与 §2「比率类指标的差异一律用 pp / bp」同一条规矩。RPC 从 0.24 到 0.25 是 **+1bp**，
   不是「+4.2%」。比率不许做滚动合计也不许做滚动均值 —— 要「一年的平均费率」
   得用**量加权**（Σ收入 ÷ Σ量），那要两条序列，不是换个函数能解决的。
   这一条也**不受本次改动影响**。

5. **近零基数的序列：判据不变，处置改成「保留同比线，但必须截轴 + 逐月点名」。**
   判据一格没动：基期 |b| < 本序列 |值| 中位数 × 0.15 的月份记为**近零基数月**；
   这种月份在窗口内占比 ≥ 1/12（平均每年一个月）→ 触发本条。
   `yoy.near_zero_base(s, win=...)` 返回 `flag`，以及**逐月的 `months` 名单**。

   ⚠️ **处置是 2026-09 页面所有者拍板改的，原话记在这里，别再按上一版执行。**
   上一版本条写的是「不画同比，画水平值」；所有者就这件事给的原话是：

   > 保留线，加截轴 + 图注警告

   所以今天的规矩是：**同比线保留**，把极端点钳住 —— **但钳在哪个字段上要看它画在哪根轴**：
   同比线画在**次轴**的（`gs_bar` 的 `ex.yoy`，本仓绝大多数）用**那条序列自己的
   `yoy.ymax`**；同比自己占**主轴**的（`ex_yoy` 那一路）才用 `ycap` / `yfloor`。
   ⚠️ **写成「一律用 ycap / yfloor」是错的**：那两个字段管的是主轴，照字面去做金线一点
   都不会被钳住（`assets/charts.js` 里左轴看 `ex.ycap` / `ex.yfloor`、右轴看该系列自己的
   `ymax`，是两条独立的分支）。两种字段的行为相同：**截轴不删点** —— 超界的点画红色
   空心圈、真值一律竖排标出（§3 公共字段表那一行）。

   ⚠️ **截轴上界不许低于末点读数。** 引擎的末点标签用的是**未钳位的原值**
   （`charts.js`：`txt(g, Xc(jy) + 5, Y2(vy[jy]) + 9.5, …)`，而 `Y2` 对 v > 上界会算到
   画布**上方**），所以上界一旦低于最新那个读数，整张图上最该被看见的数字会被画到画布外。
   实测（2026-09-03）：`jpx` Ex14 末点 +1076% 而上界取到 400 时，那个标签落在 y = −267，
   而画布只有 349.6 高。生成端因此在 `build/single.py::near_zero_guard()` 里把上界抬到
   不低于末点；代价是那两张图的上界变松（jpx Ex14 400 → 1500、enx Ex26 1000 → 1500），
   但**历史极值仍被钳住**（两张的窗内最大分别是 9,865% 与 2,156%）。
   不去改引擎，是因为 `assets/charts.js` 同时服务 34 页（文件头那条「引擎不许改」）。

   同时要在**该图自己的图注**里
   **现算列出**窗口内哪几个月是近零基数月 —— 那份名单就是 `near_zero_base()['months']`，
   别自己数、更别写死。`yoy.recommend()` 仍然回 `verdict='level'`：那是**判据的读数**，
   从这一版起不再等于处置结论，两者分开读。

   ⚠️ **代价照实写，不替它辩护。** 截轴之后读者看到的线是**被压平的** —— 压掉的恰恰是
   分母造成的那几个月，它们在线上变成贴着上界的一段，真值只能从竖排标签和图注那份
   月份名单里读。这一版是把「不给读者看一条不可读的线」换成了「给他一条读起来平缓、
   而真值在别处」的线。这是所有者选的那一侧，本条负责记下它、并要求警告写足。

   **这一条在单月口径下比从前更要紧**：流量列从前那层 12 个月滚动合计天然把一个
   近零基数的月摊进 12 个月的窗里，现在没有这层缓冲了。

   底座那一侧（接 `yoy.near_zero_base()` + 自动截轴 + 图注警告段）2026-09-02 正由
   另一名 agent 在改。**本条不复述它的状态**，也不替它定「被点名的那几个月是钳位画
   出来、还是继续留空」—— 那是实现细则，以 `build/single.py` 为准。

   量级参考 —— `db1` **Exhibit 29**（3 个月 EURIBOR 未平仓）在近零基数上相邻月
   跳 **40,608.58pp**（2024-10 → 2024-11；2026-09-02 23:4x 又复算一次，逐位相同，
   而且近 25 个月 / 2016-01 起 / 全历史三个窗口里都是窗内最大跳变）。
   ⚠️ **它在本轮的十分钟里从「两种形态都不是」变成了本条要的那一种，两次读数都记在
   这里，别只引其中一次。** 读于 2026-09-02 **23:5x** 的 `data/db1.js`：`ycap` /
   `yfloor` 都没有、图注一个近零月都没点名（上一版要的「不画同比」也没做到 ——
   金线画着），那时它两种形态都不是。它同比线上有 **36 / 127 个月是空的**，
   那是 `build/single.py` 的**逐月**近零掩码（同一个 0.15 阈值）把基期太小的月整格抹掉，
   与本条对**整条序列**下的判断不是一回事（当日实测这 36 个月与
   `near_zero_base(win=图窗)` 给的名单**逐月相同**）。窗口内还印得出来的最大读数是
   **Nov-24 +1,958.6%** —— 它的基期 6,799 张刚好落在掩码线（中位 × 0.15 ≈ 4,120 张）
   之上，所以没被抹掉。
   ✅ **同日 23:59 这一页被重建了一次，再读：图注已经按本条现算点名**
   （报出近零月占 28.3%、36 个月，并逐段列出 Jul-21…Oct-24，还写明这 36 个月的同比
   在图上是空的、断口就是它们），**次轴也截了 —— 上界 +400%**，
   走的是**这条同比序列自己的 `ymax`**（`ycap` / `yfloor` 仍是空的：那两个字段管的是
   主轴，右轴的界在序列上，见 `assets/charts.js`）。
   ⚠️ **这两次读数隔着十分钟，别把其中任何一个当现状** —— 以页面为准。
   （机检这一侧同一天 23:50 长出了 `R8_near_zero_base_undeclared`——「近零基数序列
   既没截轴、图注也没点名」就报 🟡；当次全站扫描报出 10 张、这张图在名单里，
   23:59 重建之后同一条扫描报 0 张。规则的当期形态见 §6.6 与 `--selftest`，本条不复述。）
   ⚠️ 这个例子是**存量**列，它从前走的也是点对点（第 2 条一格没动），
   所以它并没有失去什么缓冲 —— 引它只为给量级，别读成「它以前有滚动线」。
   （上一版这里写的是「Ex28」—— 2026-08 审计当日它确实是 Ex28，后来图号往后位移了
   一格；今天 Ex28 是 OAT 未平仓。这是「散文里存图号」的同一类病，与 §6.5 那批
   不标窗口的数一个道理。）
   两个阈值的推导写在 `yoy.near_zero_base` 的 docstring 里，`python3 build/yoy.py`
   的第 ⑧ 节会当场重跑 —— 2026-09-02 那次重跑的样本是 **93,556 个真实点、722 个数值列、
   712 条序列**（上一版这里写的 65,544 点 / 507 条序列是 2026-08 的样本量，
   `series/` 长了就过期）。**别在这里抄死数**：样本量随数据长，以当场重跑的为准。

### 6.2 例外：保留滚动口径的名单（`exchanges-apac` 两张、`exchanges12` 三张）

「全站单月」有一张**明文例外名单**。名单是这么来的，两级要分开记，因为它们的
权威性不一样：

- **所有者直接点名的**：`exchanges-apac` Ex5 与 `exchanges12` Ex4/7/8 —— 改口径那一轮
  单独问过、单独答的「只改折线，这些不动」。
- **由那个回答推出来的**：`exchanges-apac` Ex15。所有者没有逐张点它，但它是 Ex5
  最新那根柱的分解，而且同样不是折线；把它改成单月会让分解的两侧与被分解的总量
  不同口径、相加不再等于净额。这一条是推论不是原话，**所以它是名单上最先该被
  重新审的一条** —— 哪天所有者说 Ex5 也改，Ex15 必须跟着改。

名单上的每一张都**不是折线**：

| 页 / 图 | 图型 | 为什么留 |
|---|---|---|
| `exchanges-apac` Exhibit 5 | `grouped_bars` | 每根柱是「一整年 vs 前一整年」，三段互不重叠。它的横轴不是月，命题本身就是年度增速，换成单月无从对齐 |
| `exchanges-apac` Exhibit 15 | `bridge_bar` | **推论，非所有者原话**（见上）。它是 Exhibit 5 最新那根柱的量价分解，**滚动窗口逐字相同**（Aug-25–Jul-26 vs Aug-24–Jul-25），横轴是交易所不是月。分解的两侧与被分解的总量必须同口径，否则相加不等于净额 —— 所以它只能跟着 Exhibit 5 走。⚠️ **菱形不等于 Ex5 那根柱**（实测 JPX 67.7 vs 66.4、SGX 36.4 vs 36.4、ASX 22.8 vs 21.0）：差的不是窗口是**底料** —— 为了让分子分母同口径，Ex15 改用了更窄的列（JPX 用 `adt_cash_stocks_jpytn`、ASX 用含场外报告的总额）。这个差由 Ex15 图注逐家列出，别在别处写成「菱形就是那根柱」 |
| `exchanges12` Exhibit 4 / 7 / 8 | `range_band` / `grouped_bars` | 整页命题就是「定基名义额的 12 个月滚动合计同比」，含区间带与「张数 − 名义额」差值图；两侧必须同口径才是结构差 |

⚠️ **例外只按「所有者点名 + 命题本身不是月度」两条同时成立来给，不是按图型给。**
同样是 `bridge_bar`，别的页若拿它画月度刻度的同比，仍然要改；同样是 `grouped_bars`，
别的页画滚动同比仍然报 🔴。判据侧的名单是 `tools/check_yoy_caliber.py` 的 `ROLLING_OK`，
**与本表是同一份名单的两个副本** —— 改一处必须改两处，漏改的后果是判据放行一张
不该放行的图（假阴性，不会响）。

**名单上的每一张都必须逐处点名**（沿用上一版 §6.1 第 3 条那套写法）：凡是同一页上
既有单月图又有名单上的图，页尾「口径与方法说明」要写成「Exhibit 5、Exhibit 15：
12 个月滚动合计的同比」这种可核对的形式，并明写**不要跨口径比高低**。
`exchanges-apac` 是**唯一一页两种口径并存**的页（Ex17 与汇总表走单月，Ex5 / Ex15
走滚动）；`exchanges12` 整页都是滚动、没有单月图，混不起来，所以它只需要在副标题
里声明口径，不需要逐图点名。

### 6.3 图型豁免（补回 `range_band`；覆盖的对象与上一版那张表逐个相同）

以下这些的格内 / 柱高本来就是单月读数，或者横轴根本不是月，与本次改动无关；
列在这里只为免得有人以为它们漏改了：

| 对象 | 为什么在这张表上 |
|---|---|
| `heat_matrix` | 每一格就是一个月 |
| `seasonality` | 命题是「几月高几月低」，滚动窗口正好把季节性抹平 |
| `qtr_bar` / `bridge_bar` | 横轴不是月 |
| `range_band` | 横轴不是月（区间带对着的是期，不是月） |
| 汇总表的 `m/m` / `y/y` 两列 | 运营核对表不是趋势判断，读者拿它逐条对公司披露 |

⚠️ **`range_band` 是 2026-09 重写本节时漏抄的，这里补回，「与上一版同」这个说法
一并撤掉** —— 上一版 §6.2 的豁免表里有它（与 `qtr_bar` / `bridge_bar` 并列在
「横轴不是月」那一行），新版第一次写的时候把它落下了，而
`tools/check_yoy_caliber.py` 的 `EXEMPT_KINDS` 从头到尾都带着它 ——
契约与判据对不上，而且**错的那一侧是契约**。落下它不是无害的：全站唯一一张
`range_band`（`exchanges12` Exhibit 4，2026-09-02 全站快照实测就这一张）在契约里
因此只剩 §6.2 名单那一条路可查，读的人会以为本节与它无关；实际上这张图**同时**
被 §6.2 的点名名单和本节的图型豁免管着，两条路各管一件事（见下一段）。

⚠️ **这张表只有前四行在判据侧有对应物。** `EXEMPT_KINDS` 是一份 **kind 名单**，
只对 exhibit 生效；汇总表的 `m/m` / `y/y` 两列不是 exhibit，判据从来读不到它们 ——
最后那一行是写给人看的契约声明，不是机检得到的东西。别指望改坏了会响。

**豁免到底关掉了什么 —— 2026-09 收窄过一次，写在这里免得下一个人只能去读代码。**
判据侧 `EXEMPT_KINDS` 今天**只关掉 R4**（「单月同比没写进标题」）：这几类图的标题
本来就不该念口径，放开这一条会凭空多出几十条 🟡。
⚠️ **这个数是活的，引用前必须当场重跑，本节不再存一个可以被抄走的常量。**
**重跑方法**（照这个来，别自己发明分母）：把判据脚本复制一份到仓外，
`EXEMPT_KINDS` 置空，对同一份 `data/*.js` 重扫，数它比原版多出来的
`R4_mom_not_in_title`；按 exhibit 的 `kind` 分档看。**别改仓里那份跑**。
本轮实测：2026-09-02 21:4x 的 `data/` 快照上是 **42 条**
（`heat_matrix` 36 / `qtr_bar` 6 / `bridge_bar` **0**），
影响的图 11 张（heat_matrix 5 / qtr_bar 6）。这是一次读数，不是常量。

⚠️ **本节从前还复述过「判据脚本里写的是几」——那条句子已经删掉，理由值得记死。**
它写的是「判据脚本里那份 **69/37/26/6** 与本节这份都该改成当场重跑」。
到 2026-09-02 21:4x 去核，脚本里那份**已经不是 69/37/26/6** 了（同一天它被另一名
agent 改过一轮）—— 于是契约在描述一个不存在的脚本版本。这一条不必追究「写下的那一刻
对不对」：**两处副本各自快照、各自更新，谁也保证不了对方的状态，复述必然漂。**
**规矩：契约指向那份副本，不复述它的数。** 那几份副本在判据脚本里
（`EXEMPT_KINDS` 定义上方的注释、以及 R4 那段的行内注释各有一处，21:4x 核过），
改判据的人自己重跑、自己改。

掉下来的是 `bridge_bar` 那一档（本节记过 26 条，今天 0 条）——
原因查清了、不是数错：R4 对回源判不出口径的图有一条**据文字的降级回退**
（`mom_anywhere and not roll_anywhere`），而全站 18 张 `bridge_bar` 里
`mom_anywhere` 为真的只有 1 张（`exchanges-eu` Ex15，2026-09-02 21:4x 复算），
而那一张的 `roll_anywhere` 同时也为真，回退进不去；`identify()` 又对横截面图恒回
`caliber=None`，于是这一档现在一条都不响。那是噪声，所以没放开。其余一条都不关：

- **R1 / R2 照跑。** 豁免图若真画了滚动口径的同比，一样报 🔴 —— 这正是 §6.2 末段
  「同样是 `bridge_bar`，别的页若拿它画月度刻度的同比，仍然要改；同样是
  `grouped_bars`，别的页画滚动同比仍然报 🔴」那句话的执行者。收窄之前那里是
  `if exempt: continue`（无条件放行整个规则循环），与那句话正面冲突。收窄之后
  全站快照新增 🔴/🟡 **0 条**：豁免图型的同比序列里，回源确定命中滚动的
  是 **0 条**，而 R1/R2 只对这一档开口。（序列条数同样是活数：本节上一版记 118 条，
  2026-09-02 21:0x 复算 **117 条**——91 张豁免图上 mom 54 / 判不出 63；
  上一版记的是 mom 54 / 判不出 64。**「命中滚动 0 条」这一句才是结论**，
  条数只是它的分母，隔几张 payload 重跑就会差一两条。）
- **R3（同页并存两种口径要逐处点名）的分子里不算豁免图**，但 §6.2 名单上的图会按
  名单被单独加回「滚动」那一侧。方向要分清：**豁免图不算偏离**（它的格内读数本来
  就是单月），所以「一页里既有豁免图又有单月图」不构成混用；**§6.2 名单上的图算
  偏离**，所以「一页里既有名单上的滚动图又有单月图」照样要在页尾点名。
  ⚠️ 上一版 §6.2 的末句是「豁免的是『必须用滚动』，**不豁免**第 3 条 —— 一页里既有
  豁免图又有默认图，口径说明照样要点名」。那句话是在「滚动是默认、豁免图算偏离」
  的框里写的；框翻过来之后它的结论跟着翻，**不要照抄回来**。今天要点名的是
  §6.2 那一侧，不是豁免那一侧。

### 6.4 几条实现细则（踩过的坑，别再踩一遍）

- **同比在切窗口之前算完，再切窗。** 切完再算，窗口最前 12 期永远没有同比。
- **比较两种口径时样本必须先对齐。** 图注里报代价要拿两种口径**都有值**的月份比，
  不对齐就把样本效应混进标准差里读成口径效应。实测 `schw` Core NNA，**全历史**
  （这条列自 2017-02 起，与 2016-01 图窗是同一段）：不对齐 **4.44x**、对齐后 **4.61x**，
  差的那 0.17 倍全是样本效应 —— 单月侧 98 个月、滚动侧 91 个月，交集只有 87 个。
  `yoy.caliber_diff()` 已经先取交集再比，别自己写。
  ⚠️ **这个例子只在长窗口上看得见**：拿末 25 个月量，两种口径每个月都有值，
  对齐前后同为 5.28x，差异整个消失。⚠️ 上一版这里写的是 4.48x / 4.77x ——
  那不是算错，是 `series/schw.csv` 回填前（99 个月、Core NNA 自 2018-05 起）的实测值
  （2026-09 拿 `git show fa0dece:series/schw.csv` 重算，逐位复现 4.48 / 4.77）。
  **窗口一格没变，变的是序列长度** —— 复算对不上时先核窗口，再核底料。
  ⚠️ **「别自己写」曾有 4 个生成器没做到；这一轮正在被改掉，两个时点都记下来。**
  7 个生成器各有一个叫 `caliber_stats()` / `_cal()` 的函数，要分两拨看：
  - **`cme` / `cboe` / `hkex` 只是键名适配层**，函数体里真正干活的是
    `caliber_diff_win()` → `yoy.caliber_diff()`。**这三家一直合规**，别当违规抓。
  - **`hood` / `schw` / `lpla` / `msci` 曾是真自写。** 判据是**这四个文件里
    `caliber_diff` 出现几次**（别数行号 —— 本节原来存的 `schw.py:451` /
    `lpla.py:1077` / `msci.py:954` 三个行号，一天之内就全指到别处了）：
    · **HEAD `b8cae5d` 上：四个文件各 0 次** —— 那是「真自写」的那个时点；
    · **2026-09-02 21:5x 的工作树上：hood 13 / schw 6 / lpla 12 / msci 5 次**，
      三处流量口径（`schw` / `lpla` 的 `caliber_diff(s, yoy.FLOW, win=…)`、
      `msci` 的 `if kind == Y.FLOW` 那支）都已改成直接调共享实现。
    **这是另一名 agent 本轮在飞的改动，不在本文件的域内** —— 合并前请照上面那条
    `grep -c caliber_diff` 重核一次，别照抄这两个时点里的任何一个当现状。
    其中**存量与比率那几处情有可原**、也不在这次改动里：`caliber_diff()` 开头有个
    `if kind != FLOW` 分支，对存量与比率**根本不比**，滚动那一侧全回 `None`，
    而这几页要印的对照量恰恰是存量的「12 个月滚动**均值**同比」与比率的百分点差，
    共享实现给不了（`hood` Ex 那处、`schw` 的 ST4/ST6、`lpla` 与 `msci` 的存量处）。
  自写的那一版**都记得先取交集**（`hood.py` 的 docstring 把理由抄得一字不差），
  所以没有因此印出过错数；但四份副本就是四次要做对的判断，正是本文件开头反对的。
  要根治得给 `caliber_diff()` 补一条「存量对滚动均值、比率对 pp 差」的对照支，
  那是另一轮的活，先记在这里。
- **日均序列不要乘回交易日。** 这一条在单月口径下**更强**了：日均列的单月同比
  （日均 ÷ 去年同月日均）本身就把「今年这个月多开几天市」除掉了，乘回去等于把它
  请回来。从前的滚动合计还得先还原成当月合计，现在这一步整个消失 ——
  `level_yoy`（旧名 `ttm_yoy`）与 `mix` 的 `granularity` / `total_col` / `weight_col`
  三个 spec 键因此在 2026-09 一起删掉（`build/single.py` 的 `_norm_level_yoy` 撞上
  会硬失败）。⚠️ **删的只有这两处。`decomp` 的同名三个键不动** ——
  `DECOMP_REQUIRED` 里 `granularity` 至今必填，它服务的是「把日均列还原成当月合计
  好求年度端点」（量价分解的横轴是日历年），与同比口径无关。
- **整条同比都算不出来时不要给次轴。** 引擎只看 `ex.yoy` 在不在就判 dual，
  值全是 null 时量程退化成 [0, 1]，右边印出一列假刻度而金线一个点都没画。
  走 `single.yoy_rhs()`，它在这种情况下返回 `None`。
- **改口径会造出重复图。** 同一列若既在 groups 的单列桶里（底座画 gs_bar + 次轴
  单月同比）、又在 `level_yoy` 里，两张图从前口径不同、各有用处，现在同列同窗口同
  口径、一字不差。2026-09 因此删掉了**六条** `level_yoy`：`asx` 现货成交笔数、
  `jpx` 内国株成交股数与衍生品全品种日均名义额、`sgx` 证券市场成交股数与成交额、
  `enx` 现货成交笔数（上一版这里写的「asx / jpx / sgx 四条 + enx 一条」漏了一条，
  2026-09 拿 AST 逐条数过 `git show HEAD:build/specs/*.py` 与工作树的差）。
  护栏不在 `ex_level_yoy` 里 —— 它已经搬进 `build/single.py` 的 `Page.log_yoy_bar()`，
  五条 bar_yoy 路径 + 头条纯同比图全部走这一个入口登记并当场查重，**分两档**：
  同族 + 横轴逐格相同 → 硬失败（两张一字不差，spec 删一条）；同族但窗口不同、
  或不同族 → 只记进 `dup_yoy` 账本、构建期打印一行、页尾写明哪两张是同一条线。
  加新图之前先想一遍：这一列在本页已经有几张带同比的图？

### 6.5 存档：2026-08 全站审计的实测（结论被推翻；测量 2026-09 逐条复算过 —— 对得上的补了窗口标注，对不上的留痕）

**这一节为什么留着。** 不是因为 §6.1 第 3 条需要它 —— 第 3 条要求的恰恰相反：
每张图拿**自己那条序列**现算（`yoy.describe(yoy.caliber_diff(...))`），一个数都不引
这份存档，引了反而违反「不引别家的例子」那句话。留它的理由有三条，都不是「代价有多大」：

1. **它是「上一版契约为什么那样定」的证据链。** 哪天有人想把默认改回滚动、
   或想问「当初凭什么定成滚动」，该看着同一批数字重开，而不是从头再测一遍。
   ⚠️ **这批数在仓里有两份副本**：另一份在 `build/yoy.py` 的模块头，逐条同源。
   2026-09 只订正了本节这一份（`build/yoy.py` 不在这一轮的文件域），
   那一份仍然是**不带窗口标注**的老写法 —— 以本节为准，那边该跟着改。
   ⚠️ **`build/yoy.py` 那边今天有三处该跟着改，逐处点名**（2026-09-02 21:0x 核过）：
   ① 模块头那五条实测**一个窗口标注都没有**，而且把本节判为「复算不出来」的
   「2.08 倍」与「30pp vs 4.8pp」当**事实陈述**在写；
   ② `caliber_diff()` 的 docstring 里 `std_ratio` 那行写着「审计里的『放大倍数』，
   中位 2.08」、`medjump_*` 那行写着「审计里 30pp vs 4.8pp」—— 同样是把本节
   已判「复算不出来 / 统计量名标错」的两个数写成实况；同一段还写着
   「schw Core NNA 不对齐算出来是 4.48x，对齐后是 4.77x，差的那 0.3 倍」，
   而 §6.4 已经查明 4.48/4.77 是 `series/schw.csv` 回填前的值，今天是 4.44/4.61、
   差 0.17 倍；
   ③ `near_zero_base()` 的 docstring 里「49 个文件、30 个月度序列表、514 个列、
   65,544 个可比点」是 2026-08-07 的样本量，`python3 build/yoy.py` 第 ⑥⑧ 节
   2026-09-02 当场印的是 **68 个文件、48 个月度序列表、93,556 个点、722 个数值列**
   （同一处「只有 13 个带交易日列」今天也是 **16 个**）。
   三处都在 `build/yoy.py`，不在本轮的文件域，只记不改。
2. **它是本仓「散文里存了会过期的数」这条病的活标本。** 下面每一条都在 2026-09
   逐条复算过，结果分三档：复算得出来、但**只在退休了的旧图窗（近 25 个月）上**
   成立的（符号相反 65%、跳变量级、schw 与 jpx 那两个「最极端」）；复算得出来
   **且与窗口无关**的（schw 的 +569% vs −13%、db1 的 40,609pp）；
   以及**复算不出来**的（放大 2.08 倍、69%）。第一档换到今天的图窗
   （`build/single.py` 的 `WIN_FROM = 2016-01`；到 2026-07 满打满算 127 个月，
   短序列从自己的首月起）没有一个还对，
   根因不是算错，是**当年记数时没写窗口**。所以本节每一条都补上了窗口标注 ——
   ⚠️ **引用这批数一律要连窗口一起引**，这是这次翻车留下的那条通用教训。
3. **对不上的留痕不吞。** 复算不出来的照实写在原数旁边，不静默删、不静默改；
   统计量名标错了的，标出它到底是哪个统计量，原数原样留着。

**复算口径**（下面所有「复算」二字都指这一套，2026-09-02 在当前工作树上跑）：
分母 = 页面侧、**非豁免图上、能回源复算到某一 CSV 列**的同比序列，flow / stock /
ratio 混在一起（滚动那一侧对存量列也算，走 `yoy.ttm_yoy_unchecked`，正是判据
`build_index()` 的做法）；两种口径先取都有值的月份交集，交集 ≥ 12 个月才计。
⚠️ **这个集合的大小不写在这里，因为它每 build 一次就动**（下面「分母本身也是活数」
那段记着它在一小时之内动过一次）。要用它就照上面这段配方当场重跑；
本节各条留下的是**读数**，不是可以拿去对今天的常量。
留痕（一律带时刻，都是同一天 2026-09-02 的三次独立读数）：

| 快照 | 非豁免图判为单月 | 序列条目 | 可比序列（近 25 月 / 2016-01 起 / 全历史） |
|---|---|---|---|
| 20:4x | 223 张 | 230 条 | **212**（flow 92 / stock 108 / ratio 12）/ 213 / 213 |
| 21:1x | 224 张 | 231 条 | 213（flow 93 / stock 108 / ratio 12）/ 214 / 214 |
| 21:4x（本轮独立重跑） | 224 张 | 231 条 | 213（flow 93 / stock 108 / ratio 12）/ 214 / 214 |

（页数与图数已从审计当日的 28 页 511 图长到 **34 页 631 图**，21:4x 复算仍是 34 页
631 图；「223 张判为单月」与审计原话「223 图画的是单月同比」逐字相同纯属巧合。）

⚠️ **一个复算者踩过的坑，写在这里免得第三次**：照本节配方**换成「去重到 CSV 列」的
分母**去数，21:4x 那份快照给的是 **214 列**（flow 88 / stock 108 / ratio 18）、
可比 **196 / 197** 条 —— 与上表哪一格都对不上，于是看上去像「212 复算不出来」。
它不是复算不出来，是**换了下面第 1 条那个自由度**：分母的定义在配方里，不在直觉里。
（这一坑本轮真的有人踩到：一个独立复核者按本节配方重跑，报回来的正是
「唯一回源列 214（flow 88 / stock 108 / ratio 18）、交集 ≥12 的 196–197」，
并据此判定 212 复算不出。两组数都对，只是分母不同。）

⚠️ **这段配方 2026-09-02 补过一次 —— 原来漏了三个决定结果的自由度，照字面复算得不到
65% / 13.4%。** 四条一起写死，缺一条数就变（第 4 条是本轮补的）：

1. **「序列」按「图 × 序列条目」数，不是按去重后的 CSV 列数。** 同一列画在两张图上
   就计两次（2026-09-02 21:1x 快照：224 张图上 **231 条**序列条目、去重到 CSV 列只剩
   **214 列**）。过滤掉「对齐月 < 12」的之后，前者给 213（近 25 个月窗口）/ 214
   （2016-01 起与全历史），后者给 196 / 197。**审计那 225 条是前一种**：
   照「去重列」复算，近 25 个月窗口上月级是 653/4,793 = **13.6%**，
   对不上审计记的 13.4%（序列占比 127/196 = 64.8% 倒是碰巧还落在 65% 上，
   所以只核序列占比看不出这条自由度）。
2. **单月那一侧按列自己的 kind 取**：比率列走 `yoy.mom_yoy(s, yoy.RATIO)`（百分点差），
   流量与存量都走 `mom_yoy(s, yoy.FLOW)`（`mom_yoy` 里 FLOW 与 STOCK 走同一支，
   只有 RATIO 分叉）。**一律用 FLOW 是错的**：那批序列里有 12 条比率列，
   拿 (a/b−1) 去算它们，近 25 个月窗口上会得到 141/213 = **66.2%**、
   723/5,216 = **13.9%** —— 两个数都对不上审计记的 65% / 13.4%。
3. **相邻月跳变在原始的连续月索引上算，跨过空洞的那一对不算跳变**
   （`yoy.caliber_diff()` 的做法：把交集外的月份填成 NaN 再 `np.diff`，
   而不是把序列压缩到交集上）。压缩到交集会把空洞两侧的两个月当成相邻月，
   于是「今天的图窗上最大跳变的中位」从 **47.8pp** 变成 49.2pp。
   符号相反与标准差不受这一条影响，只有跳变类的统计量受。

4. ⚠️ **第四条自由度，2026-09-02 23:5x 补上：「近 25 个月」是谁的末 25 个月。**
   上面三条没写死它，而它是有分歧的 —— 按**每条列自己的末 25 个月**取，与按
   **全站同一段月份**（图窗末端对齐）取，落在不同的月份集合上。本轮照前一种重跑，
   得到近 25 个月那一组是 138/213 序列、月级 667/5,079，而 21:1x / 21:4x 记的是
   138/213、697/5,216：**序列级逐条相同，月级分母差了 137 个「序列 × 月」**——
   差就出在这一条。要复算「近 25 个月」那一组的**月级**数，先说清是哪一种取法；
   只核序列级看不出这条自由度（正如第 1 条那个分母，只核占比也看不出来）。
   顺带：逐序列 std 放大倍数的中位对它同样敏感（本轮 1.91x vs 上面记的 1.86 / 1.87x），
   而长窗口那两个数不受影响（2016-01 图窗 **1.40x**、全历史 **1.39x**，
   23:5x 重跑逐位落回）。

「符号相反」加 `tools/check_yoy_caliber.py` 的 `SIGN_DEADBAND_PP = 0.5`
（两侧 |同比| 都 ≥ 0.5pp 才计）。死区也是敏感的：改成 0 是全历史月级 25.3%、
改成 1.0pp 是 19.3%，而 0.5pp 才给下面那张表里 entries × 全历史那一格。
⚠️ **这三个数 2026-09-02 23:5x 照同一套配方（entries 分母 × 全历史 × 按 kind）
重跑过：1.0pp → 19.27%、0.5pp → 21.33% 两个逐位落回，而 0pp 那个复算出来是
23.82%，不是 25.3%。** 差在哪没查清（原数没标快照，也没写「死区 0」那一档是怎么
处理两侧恰好为 0 的月的），两个数都留着，不静默改。要用就照配方当场重跑，
**别拿这一行的任何一个数去对今天的仓**。

**分母按窗口分，两组不许互相换算**：近 25 个月窗口比 2016-01 起与全历史**少一条序列**
（同一批列里那条在末 25 个月内对齐月不足 12）。所以下面「近 25 个月」那一组与
「今天的图窗 / 全历史」那两组各有各的分母，绝对条数（138 / 199）本来就不该互相换算。

⚠️ **分母本身也是活数 —— 本节写定当天，一小时之内就变了一次，照实记在这里。**
2026-09-02 **20:4x** 快照：223 张非豁免图判为单月、230 条序列条目 →
近 25 个月 **212** 条（flow 92 / stock 108 / ratio 12）、2016-01 起与全历史 **213** 条。
同日 **21:1x**（另一名 agent 给 §6.1 第 3 条补图注、重跑了大半个 `data/` 之后）：
224 张图、231 条 → **213** 条 / **214** 条（flow 93 / stock 108 / ratio 12）。
**符号相反的分子一个都没动**（近 25 个月仍是 138 条序列、697 个月），
动的只有分母：212→213、5,191→5,216，于是占比从 65.09% / 13.43% 变成
64.79% / 13.36%，四舍五入还是 65% / 13.4%，但**多写一位小数就对不上了**。
同日 **21:4x** 第三次重跑（另写一份脚本、只照配方）：与 21:1x **逐位相同**
（224 张 / 231 条 → 213 / 214 条，flow 93 / stock 108 / ratio 12；
138 条序列、697 个月、5,216）—— 这一小时里 `data/` 没再动。
下面各条一律以 **20:4x 那份快照**为准（因为它才是「审计的 225 条」那一批的直接后继）；
引用时连快照时刻一起引，或者照上面那段配方当场重跑 ——
这正是本节反复在说的那件事，只不过这一次它是在一小时之内、在同一份文档里发生的。

28 页 511 图（**审计当日**的规模），其中 223 图画的是单月同比，逐序列复算、样本对齐后：

- 单月同比的逐月标准差 ÷ 12 个月滚动同比的逐月标准差：中位 **2.08 倍**。
  ⚠️ **复算不出来。** 在审计自己的分母上、旧图窗（近 25 个月）里，逐序列比值的中位是
  **1.87 倍**（20:4x 快照那 212 条；21:4x 在 213 条上重跑是 **1.86 倍** —— 差的
  0.01 是多出来的那一条序列，不是算法变了）；换个统计量读成
  「中位 std_单月 ÷ 中位 std_滚动」是 **1.98 倍**；
  今天的图窗（2016-01 起）是 **1.40 倍**，全历史 **1.39 倍**
  （这两个数 21:4x 独立重跑逐位相同 ✅）。唯一精确落在 2.08 的是
  **另一个宇宙**：「`series/*.csv` 全部流量列」在近 25 个月窗口上恰好 2.08x
  （`python3 build/yoy.py` 第 ⑦ 节 2026-09-02 当场印的就是 2.08x —— 但那也是个会随
  数据长而变的活数，别当常量抄）。而那个宇宙给不出下一条的 65%：同一节印的
  「符号相反的序列占比」是 89%。
  也就是说**这三个引用数当年不在同一个分母上**，或者「2.08」量的是中位数之比而不是
  比值的中位。两种读法都对不上审计自己写的「225 条可比序列」。原因未查清，留痕在此。
- 225 条可比序列里 **147 条（65%）** 至少有一个月两种口径**符号相反**；
  5,110 个「序列 × 月」里 686 个（13.4%）方向相反。
  ✅ **复算得出来（2026-09 复算，窗口 = 近 25 个月）**：138/212 = **65%**、
  697/5,191 = **13.4%**，两个数逐位落回原值，连分母的量级都对得上 ——
  这是本节最硬的一条，也是它把上面那条分母考证钉死的证据。
  ⚠️ 但它**只在旧图窗上成立**：换到今天的图窗（2016-01 起）是 **93%**（199/213）、
  月级 **21.2%**（4,737/22,302 = 21.240%）；全历史序列级同样 **93%**（199/213）。
  序列越长越容易找到一个符号相反的月，这个数对窗口极敏感 ——
  拿 65% 去对今天的图会当场对不上。
  ⚠️ **这一句里的「全历史月级 21.4%」复算不出来；而上一版在这里订正成的那个新绝对数
  （「引用请用 21.3%」），本轮也撤掉了。** 撤的不是算术，是**形态** —— 这个位置不该印
  一个可以被抄走的绝对值。它前后被订正过三次（21.4 → 21.3 → 又有人报回 21.2），
  每一次都在几十分钟内重新对不上，因为这组数**每重建一次 `data/` 就动**。
  下面改成「结构性结论 + 三次读数的账 + 配方」，三个数一个都不删。

  **不随快照动的那一半（要引就引这几句）**：换到长窗口（2016-01 图窗与全历史），
  「至少有一个月两种口径符号相反」的序列是**绝大多数** —— 九成以上，不是近 25 个月
  窗口上那 65%；月级占比落在**两成出头**。近 25 个月窗口比长窗口**少一条**可比序列
  （同一批列里有一条在末 25 个月内对齐月不足 12），所以两组的绝对条数本来就不该
  互相换算。序列越长越容易撞上一个符号相反的月 —— 这条单调性才是这批数的结论。

  **三次读数与各自的自由度（一律连时刻、分母、窗口一起引）**：

  | 读数 | 分母 | 窗口 | 出处 |
  |---|---|---|---|
  | 21.4% | 审计没写 | 审计没写 | 2026-08-07 审计原文；本轮排了四种读法，没有一格落在 21.4，原因未查实 |
  | **21.3%** = 6,012 / 28,190 = 21.327% | entries（图 × 序列条目） | 全历史 | 2026-09-02 的 21:1x / 21:4x / 23:4x 三次跑，逐位相同 |
  | **21.2%** = ① 5,513 / 25,964 = 21.233% ② 4,753 / 22,406 = 21.213% | ① 去重到 CSV 列 ② entries | ① 全历史 ② 2016-01 图窗 | 同日 21:4x / 23:4x |

  ⚠️ **上一版那句「另一个复核者报回的 21.2% 是去重列分母的读数」，本轮撤掉** ——
  它只说对了一半：entries 分母自己在 **2016-01 图窗**上也落在 21.2%
  （4,753 / 22,406 = 21.213%）。**21.2 至少有两条路能到（换分母，或换窗口），
  21.3 只有一条。** 一个不把「分母 + 窗口 + 死区」三样一起说出来的 21.x%，
  指不到任何一个具体的量 —— 这就是这一格反复翻车的根因，不是谁算错了。

  ⚠️ **2026-09-02 23:4x 本轮又独立重跑一次**（另写第三份脚本，只照本节
  「复算口径」那段配方）：entries × 全历史 6,012 / 28,190 = 21.327%、
  去重列 × 全历史 5,513 / 25,964 = 21.233%、entries × 2016-01 图窗
  4,753 / 22,406 = 21.213%、去重列 × 2016-01 图窗 4,351 / 20,576 = 21.146%；
  序列级 entries 200/214、去重列 184/197，两个都是 93%。与 21:4x 那次**逐位相同**
  （这两小时里 `data/` 没再动）。⚠️ 同一次跑里，「按 FLOW 一刀切」那两格是
  entries 21.717% / 去重列 21.657% —— **与上一版记的 21.74 / 21.68 差在小数第二位**，
  因为那两个是 20:4x 快照的读数。**连「复算不出 21.4」的旁证本身都在漂**，
  所以本节不再指定「引用请用哪个数」。

  **要引就当场重算，别抄上面那张表。** 配方是本节「复算口径」那四条自由度
  （entries vs 去重列 / 单月侧按列的 kind 取 / 跨空洞不算跳变 / 「近 25 个月」是谁的
  末 25 个月），加死区 `SIGN_DEADBAND_PP`。⚠️ **今天仓里没有现成入口能重算这一组数**
  （2026-09-02 23:5x 逐个看过）：`tools/check_yoy_caliber.py --audit` 当场重算的是
  **判据几道闸门的代价**（图型豁免 / `legal_mom` / 回源容差 / 登记门槛），
  不是符号相反占比；`python3 build/yoy.py` 第 ⑦ 节倒是当场算符号相反的序列占比，
  但那是**另一个宇宙**（`series/*.csv` 的全部流量列，不是页面上画出来的那批 ——
  它自己的注二就写着这件事）。所以这里给的是配方不是命令：照配方现写十几行
  （`build_index()` + `scan_pages()` 拿到 items，按那四条自由度过一遍）。
  哪天判据长出一个当场重算本组数的入口，本节改成指过去、把上面那张表删掉。
- 相邻月跳变中位：单月 **30pp** vs 滚动 **4.8pp**。
  ⚠️ **统计量名标错了，数本身对得上。** 按字面（`caliber_diff` 的 `medjump_*`，
  逐序列跳变中位再取中位）复算只有 **7.9pp vs 1.4pp**，差约 3.9 倍；
  但读成「逐序列**最大**跳变的中位」是 **26.8pp vs 4.6pp**（2026-09 复算，
  窗口 = 近 25 个月），落在 30 / 4.8 上。旁证：紧接着那条列的三个极端例子
  （schw 跳 546pp、jpx 跳 1021pp、db1 跳 40,609pp）**全都是最大跳变** ——
  审计通篇的「跳变」就是最大跳变。今天的图窗上这一对是 **47.8pp vs 8.1pp**
  （2026-09-02 复算 47.77 / 8.13 ✅ 逐位落回 —— 但**只在「跨空洞不算跳变」那种算法下**，
  见上面复算口径的第 3 条；把序列压到交集上再 diff 会得到 49.17，
  这一对是全节对那条自由度最敏感的数）。
- 69% 的序列存在某个月 |单月| ≥ 2 × |滚动|。
  ⚠️ **复算偏高 8pp**：近 25 个月窗口 **77%**，今天的图窗与全历史都是 **97%**
  （2026-09 复算）。差的这一截原因不明（分母重建已由上一条的 65% / 13.4% 钉死，
  不像是分母错）；同样没标窗口。
- 最极端：`schw` Core NNA（Aug-24 单月 **+569%** vs 滚动 **−13%**，相邻月跳 546pp）；
  `jpx` 募资额放大 60 倍、跳 1021pp；`db1` EURIBOR OI 在近零基数上跳 40,609pp。
  三条逐条复算（2026-09）：
  - `schw.csv:core_nna_usdbn` —— +569.39% vs −13.19% ✅ 逐位复现，且**与窗口无关**
    （同比永远在全历史上算完再切窗）。「跳 546pp」= 545.77pp（2024-08 → 2024-09），
    数值 ✅；但「最极端」这个定语**只在近 25 个月窗口内成立** ——
    2016-01 图窗与全历史里这条列的最大跳变是 **1,381.74pp**（2021-07 → 2021-08）。
  - `jpx.csv:ipo_funds_jpybn` —— 「放大 60 倍」= **60.4x**，是**月级
    |单月| ÷ |滚动| 的窗内最大值**（2025-08：单月 +955.45% vs 滚动 −15.82%），
    ⚠️ **不是**本节抬头那个 std 之比（这条列的 std 比是 10.3x）——
    同一段里「放大 N 倍」承担了两种定义，读者无从分辨，这本身是缺陷。
    「跳 1021pp」= 1,020.68pp（2025-07 → 2025-08）✅ 数值复现，但它**今天已不是窗内
    最大**：2026-07 的新数据带来 1,169.39pp。两个数都只在近 25 个月窗口 + 审计当日的
    数据版本上成立；2016-01 图窗与全历史分别是 383.0x 与 9,244.51pp。
  - `db1.csv:oi_euribor3m_contracts` —— **40,608.58pp**（2024-10 → 2024-11）✅
    逐位复现，而且**三个窗口里都是窗内最大跳变**。「在近零基数上」也成立
    （全历史近零月占 17.1%，`flag=True`）。这一条经得起复算，与窗口无关。

**读这份数据的正确方式**：它说明单月同比毛刺大、且单看一个月能把方向读反 ——
所以第 3 条要求每张图把这笔账印在旁边（**用它自己的序列现算，不引本节**），
第 5 条要求近零基数序列必须截轴并把近零月逐月点名
（⚠️ 上一版这句写的是「干脆别画同比」，那是第 5 条被改之前的处置 —— 2026-09
所有者拍板改成「保留线，加截轴 + 图注警告」，见第 5 条）。
它**不**说明单月口径不该用：所有者要的就是这个能被自己核对的数，而
「同一页混用两种口径」那个真正的病根，统一成单月同样解决了。
而它**新**说明的一件事是：一个不带窗口标注的统计量，几周之内就会变成假话 ——
审计是 2026-08-07 记的，图窗 2026-08-19 就拉长了，这批数当场失效而没有任何东西会响。

⚠️ **本节与 §6.2 订正掉的句子，逐字同源的副本曾留在仓里两处，两处 2026-09-02 都已补上**
（**别在这里存行号** —— 判据脚本当天改过好几轮，本节原来写的 `:88` 到 21:5x 已经指到
别处了，`build/exchanges_apac.py:348` 也早已位移到 384；指位置要用**符号名**，不用行号）：

- 判据脚本 `ROLLING_OK` 里 `('exchanges-apac', 15)` 那一条的行尾注释曾写着
  「窗口逐字相同、**菱形就是那根柱**」—— 后半句正是 §6.2 那张表**逐字禁止**的说法
  （实测 JPX 67.7 vs 66.4、SGX 36.4 vs 36.4、ASX 22.8 vs 21.0，差的是底料不是窗口；
  这三对 2026-09-02 21:5x 复算逐位相同 ✅）。同一句在 `build/exchanges_apac.py` 里
  先改对了，判据脚本那一处是漏网的第三份副本。
  ✅ **2026-09-02 22:0x 再核，判据脚本那一处也已由该文件的负责人改对**
  （现在写的是「菱形不等于那根柱」）。该文件不在本轮的文件域，**这里记的是 22:0x
  那一刻的状态，之后以脚本为准**。
  ⚠️ 留痕：**同一句话在这个仓里有过三份副本，而且一度两两对立** ——
  这正是本节反复在说的病，也是「契约指向副本、不复述副本」那条规矩的来处。
- ~~`HANDOFF-2026-08-13.md:5`（2026-09 追记那一段）写着「页面上一条 12 个月滚动合计的
  同比都不画」，**把 §6 抬头那句话的例外从句整个丢了**~~ —— **2026-09-02 21:5x 已补上
  例外从句**（那一段现在把 §6.2 的名单连同「那五张都在横截面页、与该文覆盖的
  半导体 7 页无关」一起写明了）。留痕：去掉从句之后它曾是一句**假话**，
  因为 `exchanges-apac` Ex5/Ex15 与 `exchanges12` Ex4/7/8 今天仍然在画滚动同比，
  而且是契约批准的 —— **抄这句抬头话到别处时，例外从句不许省**。

### 6.6 自动判据

`python3 tools/check_yoy_caliber.py` 扫 `data/*.js`，对每一张含同比的图判：
🔴 未在 §6.2 例外名单里、却画着滚动口径的同比；🔴 存量序列的同比被称作「滚动合计」；
🟡 一页里两种口径并存（滚动那一侧来自回源复算或 §6.2 名单），而页尾口径说明没有把
滚动那几张逐处点名；🟡 单月同比没写进标题；
🟡 §6.2 名单上的**某一张图**不见了、或不再自称滚动口径；
🔴 §6.2 名单上的**整页**这一轮没扫到（页被删或改名，见下面「为什么这一条要停机」）。
有 🔴 退出码非 0。
（另有一条与口径无关的管道规则 `R0_page_unreadable`：某个 `data/*.js` 根本解析不了时
报 🟡 —— 「这页很干净」与「这页没读进来」必须分得开。）

⚠️ **规则清单会长，本节不复述规则体 —— 它在本轮的两小时里就长了两条。**
2026-09-02 23:5x 读脚本时，上面这段之外至少还有 **R7**（`R7_ratio_yoy_as_pct_change`：
比率列的同比被画成「百分比的百分比」而不是百分点差 —— §6.1 第 4 条的机检面）
与管道侧的 `R0_scanner_blind_field`（判据自己读不到某张图上的某个数据字段，
是它的自体检）；同日 23:50 再读，又多了 **R8**（`R8_near_zero_base_undeclared`：
近零基数序列既没截轴、图注也没点名近零月 —— §6.1 第 5 条的机检面，🟡，不停机）。
要看这一轮到底有哪几条规则、每条还响不响，跑 `--selftest`：它逐条印规则名与
正反用例，**那才是当期的清单**，本节这两句只是读它的时刻。

**这几条各自拿什么当证据**（别笼统说「都靠回源复算」，2026-09 已经不是这样）：

- **R1 / R2 只认回源复算**，而且只在回源**确定命中滚动**时才开口 —— 判据自己拿不准
  就不喊。
- **R3** 的「单月」那一侧只认回源；「滚动」那一侧有**两个**来源：回源复算，
  加上 §6.2 那张名单的注入（`ROLLING_OK`）。两个都不是页面文字。
  「有没有逐处点名」那一步才读 `notes` —— 但那是在检查文字，不是拿文字判口径。
- **R4** 除回源之外还有一条据文字的回退（图注里说了单月、且哪儿都没提滚动）——
  这一支只用来降级报 🟡，不会把一张图判成滚动。
  ⚠️ 这条回退**会被 §6.1 第 3 条要求的那段代价文案反过来关掉**：`describe()` 印出的
  句子里带「12 个月滚动同比」，`_ROLL_DECL` 命中 → `roll_anywhere=True` →
  回退不成立。所以「图注写全了口径」的图反而走不了这条降级路，只能靠回源。
  §6.3 里 `bridge_bar` 那一档（本节记过 26 条 🟡）掉到 0 就是这个机制。
- **R5 判自述**，不判数。

⚠️ **§6.1 第 3 条（逐图印单月口径的代价）在判据侧目前是零覆盖。**
逐条读过判据当时的全部规则体（2026-09-02 21:0x 读到 R5，23:5x 连后加的 R7 / R8 一起再读）：
没有任何一条查「图注里有没有这段代价」。
判据脚本自己也承认这件事 —— R1 的报错文案里写着「并按 §6.1 第 3 条把单月口径的代价
印进图注」，那是**给人的改法建议**，不是一条会响的规则；`sign_flips()` 留在文件里，
但注释明写「不拿它报 finding（那件事归 §6.1 第 3 条，由每张图的图注自己印）」。
后果是 §6.1 第 3 条剩下的那批欠账**全站全绿、退出码 0**，与「都付清了」在输出上
一模一样（2026-09-02 21:0x 快照：判据报 `🔴 0 🟡 0`，而同一份快照上第 3 条还欠着
10 张 —— 判据一个字都没提）。**建议的补法**（要加规则的人照这个来，别自己发明分母）：

    R6_mom_cost_not_printed 🟡
      触发：exhibit 上有一条同比序列，回源命中的列被 yoy.classify() 判为 FLOW，
            而该图自己的 note / src_extra 里既没有实测代价段、也没有降级说明。
      证据：回源（不是文字）定 flow；文字只用来判「印没印」——
            与 R3 同一个方向：拿文字当被检查的对象，不当口径的证据。
      为什么必须 🟡 不是 🔴：回源判不出列的图（派生量）进不了分子，
            这条规则天生漏报；漏报的规则不该有停机的权力。

**别把这条写成「note 里有没有『标准差』三个字」** —— 那是今天几个生成器碰巧共用的
措辞，不是契约要求的东西；契约要求的是「逐月标准差 + 相邻月最大跳变（带月份）+
符号相反的月份数」三样，判据该查的是这三样都在，最省事的写法是让生成器把
`yoy.describe()` 的输出原样嵌进去、判据认那段的结构。

⚠️ **R5 不是凑数的。** §6.2 名单上那五张全是横截面图 —— 序列长度就是成员数
（4 根柱），横轴是交易所名不是月，回源根本对不齐。2026-09 之前登记门槛是 6 个点，
这几张连**进不进规则循环**都轮不上；门槛降到 1 之后它们进得来了，但
`identify()` 照样回 `caliber=None`。两种情形下 R1 **在它们身上都不会响**，
于是白名单一度是彻头彻尾的死代码：它从没在真实快照上压制过任何一条 finding，
也就意味着这几张图哪天被改成别的口径、被删掉、或图号位移，
**没有任何东西会响**（2026-09 一次对抗普查抓出来的）。R5 退一步判
**自述**：名单上的图必须还在、且必须还声明着滚动口径。自述能被改坏，所以它弱；
但「白名单被静默架空」从此是会响的。`--selftest` 里为它准备了两个用例，且用例的
页名必须是名单上真有的页，否则规则直接跳过、用例自己也白写；另有一条
「白名单必须压住 R1」的压制用例（拿 `exchanges12` Ex4 的桩件），
配一条「同一张图换到名单外的页就该报 🔴」的正例 —— 压制机制本身从此也是被验的。

⚠️ **`R5_whitelist_page_missing`（名单上的整页没扫到）定为 🔴，要能让整轮停机。**
**这一句是契约这一侧的规矩**（和 §6.1 第 3 条一样，条文比现状严是有意的）。
判据侧当天正在被另一名 agent 改：22:0x 核时那一条还是 🟡、退出码 0，
22:09 再核已经写成 🔴（当时脚本仍在编辑中，跑不通，没能实测）——
**别把这两个时刻里的任何一个当现状，以脚本为准**。
为什么单独给它停机的权力，三条：

1. **它守的正是「除了它没有任何东西会响」的那个洞。** 名单上五张全是横截面图，
   `identify()` 对它们恒回 `caliber=None` —— 页被删、或页名一改，R1 不会响
   （它只在回源确定命中滚动时开口）、R5 的逐图那一半也不会响（那一半嵌在
   `check_page` 里，页文件没了就没有哪一次调用轮得到它）。判据脚本
   `check_whitelist_pages()` 的 docstring 里记着两次构造验证（删掉
   `data/exchanges12.js` / 改名成 `data/exchanges-12.js`）：**加这条规则之前，
   两种情形的输出都是「🔴 0 🟡 0、无硬错、退出码 0」**。加成 🟡 之后多了一行日志，
   退出码仍是 0 —— 这只是把「无人知晓」挪成「印在没人读的日志中段」，
   离「会响」还差一步。
2. **它不会误报，所以停机的代价是有界的。** 这一条是集合相减（名单上的页名 −
   这一轮实际扫到的页名），既不依赖回源、也不依赖文字：要么那个页名在扫描集合里，
   要么不在。`--page` 划了范围时本来就不跑（那是使用者自己缩的圈），读不出来的页
   走的是 `R0_page_unreadable` 那条 🟡，不会混进来。
   **对照 R6 为什么必须 🟡**：R6 天生漏报（回源判不出列的图进不了分子），
   漏报的规则不该有停机的权力；R5 这一条在自己的判据上不漏报也不误报。
   这就是本仓给 🔴 的门槛：**不会误报 + 别的规则接不住**。
3. **停机之后要做的事本来就是必做的。** §6.2 已经写死「名单是同一份的两个副本，
   改一处必须改两处」。整页真被删或改名时，`ROLLING_OK` 与 §6.2 那张表本来就得跟着
   改 —— 让这一轮停在这里、逼人把契约那一侧一起改完，正是这条耦合的执行方式，
   不是误伤。

⚠️ **R5 另外两条（`whitelist_exhibit_missing` / `no_longer_declares_rolling`）
维持 🟡，不跟着升。** 它们判的是**自述**（图还在不在、还念不念滚动口径），
而自述会因为无关的改写而变；更要紧的是那两种情形下**页还在被扫**，
R3 的滚动侧注入照常工作，白名单不至于指着一片虚空。
（`exhibit_missing` 该不该也升 🔴 没定论 —— 图号整体位移时它会响，而那时页仍在扫、
证据链没断。谁要升，先按上面那两条门槛把「会不会误报」量出来再说。）

⚠️ **「白名单是死代码」这句话今天只对 R1 成立，对 R3 已经不成立了。**
同一轮里 R3 的滚动侧改从 `ROLLING_OK` 注入，于是名单成了 R3 在
`exchanges-apac` 上唯一的滚动侧证据来源。2026-09-02 实测：把该页 `notes` 清空，
R3 当场报 `ttm=[5, 15] / mom=[17] / missing=[5, 15]`；再把 `ROLLING_OK` 清空，
R3 立刻闭嘴。所以今天删掉名单里的一行，是真的会让一条规则瞎掉。

⚠️ **两处已知限制 —— 判据不是全覆盖的，洞写在这里，那正是这份契约的用处。**
（下面两条都读/跑在 **2026-09-02 23:4x–23:51** 的工作树上，**逐条带着自己的时刻** ——
这半小时里判据脚本被另一名 agent 改过一轮，第 1 条正是在这中间被补掉的。
判据脚本不在本文件的域内，本节记的是**那几刻**的状态；仍在不在，以脚本为准。）

1. **`R5_whitelist_page_missing` 的等级曾挂在「文件在不在」，不是「这一轮真查过这一页」
   —— 这个洞在本轮进行中被判据侧补上了，两个时点都记下来。**
   从前 `main()` 喂给 `check_whitelist_pages()` 的是**这一轮 glob 到的文件名集合**：
   文件存在就算「扫到了」，哪怕它根本没被解析进来。于是「名单页丢了」的三种形态里
   只有一种停机 —— **23:5x 亲手构造验证过**（把 `data/` 复制到仓外临时 root、
   `series/` 软链回来，再把 `data/exchanges12.js` 写坏成半截 JSON）：
   · 删掉 / 改名 → 🔴 1 条 `R5_whitelist_page_missing`、**退出码 1**；
   · **文件还在、但解析不了** → 🔴 0 🟡 1，只有一条 `R0_page_unreadable`、**退出码 0**；
     名单那半边一个字都没说，而那一页上 §6.2 的三张滚动图这一轮**没有任何规则看过**
     —— 与「这页很干净」在输出上一样；
   · `--page` 划了范围 → 按设计不跑（那是使用者自己缩的圈，不算洞）。
   ✅ **23:50 同一个构造再跑一次：中间那一种现在报 🔴 + 退出码 1 了。**
   判据侧给 `scan_pages()` 加了第四个返回值（「这一轮成功解析并跑完规则的页」），
   R5 的停机条件从「文件在不在」换成了「这一页进没进过规则循环」——
   正是上面那句「答非所问」该有的改法。⚠️ **这两个时点隔着二十分钟，别把其中任何一个
   当现状**：合并前照上面那段构造重跑一次（三种形态各跑一遍），以脚本为准。

2. **R4 判「标题里有没有写单月」，而同一段文字命中 `_STOCK_TXT` 就能把 R4 整条关掉，
   且这条豁免通道不出现在报表里。** `legal_mom` 的存量身份有三个来源，其中一个是
   **图题 / 纵轴 / 图例的文字**（`says_stock`：存量、期末口径、月末、未平仓、市值、
   余额…）。文字是**被检查的对象**，这里却当了豁免的证据 —— 于是一段与该序列无关的
   板块标题就能替它把 R4 关掉。**23:5x 与 23:51 各测一次**（中间隔着判据那一轮改动），
   两次都是同样 **4 条**同比序列的 R4 被**文字这一路单独**关掉（回源命中的列被
   `yoy.classify()` 判成 FLOW、列名也不是无歧义存量，即拿掉这一路就会当场报 🟡 R4；
   **这个条数是活数**，加一张图就会变，要用当场重数）：
   · `jpx` Ex13 / Ex14 —— 板块标题「东证**市值**与上市融资」，而两条序列是
     **当月**公开募集件数与**当月**募资额（`ipo_public_offerings` / `ipo_funds_jpybn`）；
   · `db1` Ex13 —— 标题「…担保品与现金**余额**」，序列是 Clearstream 结算笔数；
   · `enx` Ex34 —— 命中的是「**月末**」，**这一条文字是对的**（那列真是月末挂牌基金只数），
     错的是 `classify()` 把它判成流量 —— 同一处已记在 §6.1 第 3 条那段里。
   报表这一侧：主报告里那张「被判据猜是存量而挡掉 R4」的清单，条件里带着
   `not says_stock`，**文字这一路被明确排除在外**；`--audit` ② 会把它们算进
   「存量」那一档的总条数，但那一档不区分「文字说的」与「列名判的」。
   所以这个缺口今天是**静默**的：它既不报，也不在任何一处清单里露头。

它**不看文字判口径** —— 文字声明是被检查的对象，不能同时当证据。判法是把
`series/*.csv` 每一列按两种口径各算一遍，拿数值去比对图上那条线，命中哪种就是哪种；
派生量（ADV × 交易日、多列相加、FX 换算过的）匹配不上任何原始列，一律判「未确定」
**只计数不报错**。宁可漏报，不制造噪声。
（唯二的例外已在上面点名：R3 的滚动侧会注入 §6.2 名单 —— 那是契约的名单不是页面的
自述；R4 有一条据图注的降级回退。⚠️ 另有一处**不判口径、但拿文字关规则**的：
`legal_mom` 的 `says_stock` 那一路，见上面「已知限制」第 2 条。）

`--selftest` 对着已知错例跑一遍，确认规则还会响 —— 「今天没报错」与「规则坏了」
在输出上长得一模一样，只有这样才能分开。它同时反向验两处**压制**（§6.2 白名单与
§6.3 图型豁免）也没失效：每条压制用例都配一条「换个页名 / 换个 kind 就该响」的正例。

**目前是独立脚本，还没接进 `build/payload_guard.py`**（接进去的建议改法写在
**判据脚本自己**的末尾 —— `tools/check_yoy_caliber.py` 的 `HOW_TO_WIRE_IN` 段，
不在 payload_guard 里，那边没有这个段落）。接的方向是让生成器把口径写成结构化字段
（`ex['caliber'] = 'ttm' | 'mom'`），payload_guard 侧只做零依赖的结构检查，
回源复算那一半留在本脚本里当离线校验 —— 自述字段能被改坏，回源复算不能。
